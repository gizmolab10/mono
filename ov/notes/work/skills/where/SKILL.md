---
name: where
description: Name the one file that owns a topic or fact, before anyone edits it. Use when about to tweak a truth, or when unsure where something lives. Triggers: "where <topic>".
---

# where

Read-only.

Read `memory/shared/truth/protocol.md`, section "Finding where to tweak", and run its lookups in order, stopping at the first hit.

Reply with the one owning path — and the specific line, if a specific fact was asked for. If two files both claim the topic, that is a duplication bug: report both paths and recommend which should own it.
