---
name: propose
description: Put an idea forward without making it true. Use for design ideas, hunches, and open questions that should be captured but not yet believed. Triggers: "propose <idea>".
---

# propose

Write skill — every run logs. Truth files are never touched.

Read `memory/shared/truth/protocol.md`, sections "Studio" and "Session protocol", and capture the idea where they direct — `studio/ideas.md` or an `I:`/`Q:` log line — in the user's terms or plain language (new terms go through `define`).

Reply with one line saying where it landed. Promotion into a truth happens only at `settle` or on explicit instruction — never inside this skill.
