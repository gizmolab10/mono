---
name: start
description: Load the prompt cache — orient on the memory system before any work. Use at session start, on project switch, or whenever the session seems to have drifted from the rules. Triggers: "start", "start <project>".
---

# start

Read-only. Never edits the memory system.

Read `memory/shared/truth/protocol.md`, section "Session protocol" (Start), and execute it exactly — it names the files, the order, and the orientation budget. The file is the procedure; do not run it from recollection.

Reply with a three-line orientation: the project's current state, the truths loaded, and its unsettled `Q:` lines. If the budget is exceeded, say which files are fat instead of silently skipping any.
