---
name: settle
description: Run consolidation on a project — settle its log into the truths, ending in one labeled commit. Use when a log passes its limit, before a major work burst, or on request. Triggers: "settle", "settle <project>".
---

# settle

Write skill — every run logs and ends in one labeled commit.

Read `memory/shared/truth/protocol.md`, section "Consolidation", and execute all of its steps exactly, in order, through the final labeled commit. The file is the procedure; do not run it from recollection, and never summarize prose into vaguer prose.

Finish by reporting the settle manifest — each log line and where it went — so the settle can be audited at a glance. For a large settle, recommend a fresh-context `check`.
