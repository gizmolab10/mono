// The sections inside the details column, in the order they show. Which ones are
// open is saved as a list of these words (an empty list means every one is shut).
export enum T_Details {
	preferences = 'preferences',
}
