# ov — Overview

> a barebones web app: a details column on the left, an accent color picker inside it, and every size and color coming from one place.

Ported from ji. The proposal that describes what belongs here — and what deliberately does not — lives at `ji/notes/work/proposals/ov.md`. Read it before adding anything.

Read `notes/guides/pre-flight/lexicon.md` at session start. It names everything in this project: use those words in prose, comments, log lines and test names, and invent no synonyms.

The file map is `notes/guides/map.md`. Read it instead of globbing, and update it when files are added, moved, or removed.

Nothing about documents, tags, storage, or chat belongs in this project.
