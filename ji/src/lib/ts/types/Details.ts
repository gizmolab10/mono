// The sections inside the details region, in the order they show. Which ones are
// open is saved as a list of these words (an empty list means both are shut).
export enum T_Details {
	preferences = 'preferences',
	data        = 'data',
}
