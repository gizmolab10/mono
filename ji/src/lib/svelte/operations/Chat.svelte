<script lang='ts'>
	import { debug } from '../../ts/common/Debug';

	// Stub: the chat operation — a running history of questions and answers over the
	// store. Not built yet; this just marks the spot so the "chat" segment has a home.
	debug.log('Chat view opened — stub only, not built yet.');
</script>

<div class='chat'>chat — not built yet</div>

<style>
	.chat {
		opacity   : var(--opacity-label);
		font-size : var(--font-label);
		padding   : var(--gap);
	}
</style>
