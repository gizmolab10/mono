# ai

The host that brings the ai specialty to kb: a browser and editor for every markdown file in this repo, the memory system's. Made 12 September 2026 at step 4 of the plan in [music and ai](../kb/zone/music%20and%20ai.md), an empty host: main.ts does what ov's does, App.svelte draws kb's page, Customizations.ts holds the name, and the two bridges, `common/Core.ts` and `common/Kb.ts`, are the only files that reach through an alias, with `common/Convert_Preferences.ts` since step 9 for kb's customizations alone, panel's carried for the build and named by no file. Registered in ports.json at 5187 with a db entry naming `ai.db`, made from `ov.db` on 13 September 2026 less the rows of a removed folder, in mono's workspaces and in servers.sh. In the hub page under H, since a is AnythingLLM's. Its CLAUDE file is not in the dispatcher's listing until step 6. ov is frozen: every further change is made here, or in kb once the piece is kb's, never in ov.

**Current state:** drawing kb's page and filling kb's facts before anything mounts, name, prefix, host, hierarchies and the build notes table, `src/lib/md/builds.md`, since step 5, 13 September 2026; it hands kb the edit filter section since step 10, 14 September 2026: ai's information rows with the title's two tools, `src/lib/svelte/content/Edit_Fields.svelte`; and the operation view since step 11, the same day: the words drawn as a page, `src/lib/svelte/content/Edit_Markdown.svelte`, with the drawing in `src/lib/ts/utilities/`, and composing labels beside it since step 12, `src/lib/ts/utilities/Labels.ts`; and the search row since step 13, 15 September 2026, `src/lib/svelte/content/Search.svelte`, wired to the drawer in App.svelte. Since step 6, `plugin.py` in mono/ai is the dispatcher's plugin for ai and for ov, holding the listing rule, and since step 8 the reading and writing of a file and of its label block. Since step 7 its Customizations.ts holds the five kinds, the 39 tags and the ten tag areas, handed to kb. Since step 9, 14 September 2026, Convert_Preferences.ts sets kb's facts ahead of kb and every remembered value is saved under ai_, what was saved under ov_ moved once. Check clean at 534 files, 15 tests passing, and the build resolves.

## Zone

- [AI memory redesign.md](zone/AI%20memory%20redesign.md) — a design for a persistent memory that carries context across chats, survives consolidation and supports several long-term projects; core's until 14 September 2026.
- [adopting the ai memory design.md](zone/adopting%20the%20ai%20memory%20design.md) — what is done so far in adopting the design; shared's until 14 September 2026, as are the next seven.
- [inception into the new design.md](zone/inception%20into%20the%20new%20design.md) — the death list, and the merges of duplicated files.
- [publish — can an AI actually learn?.md](zone/publish%20—%20can%20an%20AI%20actually%20learn?.md) — the whole design explained for someone new to AI, written to publish.
- `work/` — [claude code.md](zone/work/claude%20code.md), where the toolkit actually runs and installing Claude Code; [claude session output.md](zone/work/claude%20session%20output.md), where a second, unsupervised session writes what it says; [fable.md](zone/work/fable.md), what happened with the model; [rewrite the guides.md](zone/work/rewrite%20the%20guides.md), the plan the unsupervised sweep executed; [truth and zone and design?.md](zone/work/truth%20and%20zone%20and%20design?.md), three ideas weighed in one sitting, the review copy.

## Truths

- [lexicon.md](truth/lexicon.md) — ai's own words, kb's lexicon applying whole beside it.
- [using rules.md](using%20rules.md) — how to use the rules section of the details column: what a rule reads, the regex, the label it gives, and when the dispatcher runs it.
- [map of ai files.md](truth/map%20of%20ai%20files.md) — every source file in ai; read it instead of discovering files using regex and wildcards, and update it when files move.

## More

