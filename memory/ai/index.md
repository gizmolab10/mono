# ai

The host that brings the ai specialty to kb: a browser and editor for every markdown file in this repo, the memory system's. Made 12 September 2026 at step 4 of the plan in [music and ai](../ov/zone/music%20and%20ai.md), an empty host: main.ts does what ov's does, App.svelte draws kb's page, Customizations.ts holds the name, and the two bridges, `common/Core.ts` and `common/Kb.ts`, are the only files that reach through an alias, panel's carried for the build and named by no file. Registered in ports.json at 5187 with a db entry naming `ov.db`, ov's own, until ov is retired, in mono's workspaces and in servers.sh. In the hub page under H, since a is AnythingLLM's. Its CLAUDE file is not in the dispatcher's listing until step 6. ov is frozen: every further change is made here, or in kb once the piece is kb's, never in ov.

**Current state:** drawing kb's page and filling kb's facts before anything mounts, name, prefix, host, hierarchies and the build notes table, `src/lib/md/builds.md`, since step 5, 13 September 2026; it hands kb no snippet yet. Check clean at 532 files, 5 tests passing, and the build resolves.

## Truths

- [lexicon.md](truth/lexicon.md) — ai's own words, kb's lexicon applying whole beside it.
- [map of ai files.md](truth/map%20of%20ai%20files.md) — every source file in ai; read it instead of discovering files using regex and wildcards, and update it when files move.
