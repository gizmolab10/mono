# Decisions

## Decisions made during 2026

- 13 September 2026; **panel's details column sits on the accent**, as ov's does, in place of the page color panel chose, so kb's column looks as ov's did: its stack paints the page color, the accent shows above and below. In panel's Details.svelte.
- 13 September 2026; a configured **hierarchy names its label and its order within a group**, which music's track order needs. 5b of the plan's step 5, and adopt kb's type.
- 13 September 2026; **panel uses core's status line**, which carries the offer with ontake, onhide and onreport, in place of its own line of words, so kb's offer passes through panel. A change to panel, which gallery, lv, mj and mu draw. 5c of the plan's step 5, and adopt kb.
- 13 September 2026; **browse has filters above its list of files**: search first, then collection, kind and tag, and a host's browse section, where a host hands one, is a filter among them, **after tag**, last above the list. In the plan's step 5, substep 5a, and adopt kb.
- 13 September 2026; in panel's controls row, **the name yields** to the file's section while a file is open, since kb's row holds the count, the steppers, the folders, the name field and the four buttons across it, and panel keeps the name in the middle. In the plan's step 5 and adopt kb.
- 12 September 2026; the ai host's entry file is **main.ts**, lowercase, as ov's and mu's are, not Main.ts as lv's and gallery's are. The plan's steps 4 and 5 and adopt kb say so.
- 12 September 2026; **plugin stays**, for a host's code that the dispatcher imports and runs, `plugin.py` in the host's folder. Five words were weighed against it, specialist, envoy, theme with skin, specifics and custom, specialty with purpose for the whole, and savvy, and their pacs are removed.
- 12 September 2026; **label stays**, and belongs in the shared lexicon, whose entry now says what it is: a name and a value on a file with who wrote it, a row in the db. Nine words were weighed against it, key, trait, field, aspect, attribute, fact, property, tuple and kvp, each with a pac below, and none replaced it.
- 11 September 2026; **one db per host**, beside the dispatcher: `tools/hub/ov.db` is ai's, keeping its name until ov is retired, `tools/hub/mu.db` is mu's, one ignore line for all. open_db takes the file, PLACE stays ov's and the default, so the three tools change nothing. A path stays unique within its db, so step 3 rebuilds no table and re-keys no row. The dispatcher keeps a short list of hosts, each with its folder and its db, and picks the db by the collection a request names. Built 12 September 2026 as step 3: the db is picked by the host a request names, ov when it names none.
- 12 September 2026; **ov is frozen forever**: every further change is made to ai, never ov. ov stays as it is, the static reference kb with the ai specialty is proved against, working features rows 1 to 89.
- 12 September 2026; **ai on kb first**, reversing music first of the day before: the answered questions, the backup and one db per host lead, steps 1 to 3, then the strip of ov, 4 to 19, the plugin mechanism arriving at step 6 with ai's plugin, then music, 20 to 24, then kb's new work, the drop box, mu adopts kb and three for later. The cost accepted: until music's steps run, what music needs of kb is a guess, and the hand-over is designed from ov and ai alone.
- 11 September 2026; the hand-over's design, the type kb declares and its four snippets, is written **now**, as a proposal in ov's zone, and moves to memory/kb at step 4 of the plan, so step 5 starts with it.
- 11 September 2026; music's **eight** dispatcher-side questions, under the music definition's collection information, keys and plugin, are answered before the collections table, step 3 of the plan, begins, so it, the plugin and the listing rule are built for two. Narrowed the same day: step 3 keys a file on its collection and its path alone, and step 20 on the listing rule, so four answers come before step 3 and the title's home, the artist's home and the tag reader wait as substeps of step 22, the first step that writes them.
- 11 September 2026; the music library is the volume **`/Volumes/muice myoozk`**, an NTFS disk over USB, 4.5 TB with 3.2 TB used, four top folders, Any Audio, HD Audio, MVE and MuSC-V, and 18,219 files outside the recycle bin and the system folder, counted that day: 6227 flac, 3085 jpg, 1954 mp4, 1421 vob, 733 ifo, 732 bup, 627 mpg, 479 txt, 419 rar, 299 zip, 233 shn, 231 avi, 217 wav, 161 cue, 154 log. It is the real root that step 21 proves against.
- 11 September 2026; music first's step 1, four answers: every dropped folder is a **collection of its own**, for now, **named for the folder**; a song takes **no tags**; several artists on one file are **one row each**. Four more wait as substeps of the steps that need them, the title's home, the artist as source or label and the tag reader under step 22, and the threading proposal as 21e.
- 11 September 2026; **music first**, the proposal in zone/music and ai.md, is the order: its eight steps on the dispatcher side, the dispatcher-side questions under the music specialty answered as its step 1, nine once the library was counted, then ov strip down, steps 9 to 24, its old step 3 reduced to the ai plugin, step 11, then kb's new work, 25 to 27, then the drop box, 28. Steps 9 and 10 may go ahead meanwhile. Reversed 12 September 2026: ai on kb first, above.
- 25 August 2026; new tag **keep** in the **progress** tagset.
- 25 August 2026; new tag **maybe** in the **fix** tagset.
- 25 August 2026; **speed** renamed **faster**, everywhere it was worn.
- 25 August 2026; a ninth tagset, **fate** — keep, maybe, stale, stow — its four tags moved out of **progress** and **fix**. Each tag answers: what becomes of this file?
- 25 August 2026; the **stow** tag is gone — no file wore it. The tagset stays **fate**: keep, maybe, stale.
- August 25. CLAUDE files are **lowercase** by convention, and yet **uppercase** is still tolerated.

## Evaluations (pac) made during 2026

- 12 September 2026; **rename label to kvp, for key value pair, for a kind or a tag on a file.** For: it says exactly what the row is, a key and a value, and nothing else, where label says a screen's text and tuple says a count. Three letters, shorter than label, and the shape of the repo's own names, ov, kb, mu, ai, ji, so it reads as one of them. The two specialty tables already head their first column key, so kvp's key is a word the plan uses for the same thing. Nothing in the code or the memory uses it, so a kvps table collides with nothing. Against: it is not a word but three letters, and the always rules ask for everyday or standard technical English, which kvp is only among programmers who spell it out, so every reader outside that room needs it spelled out once, and the lexicon must define it in the same write. It cannot be said aloud as a word, kay vee pee, and it takes no plural without reading as a typo, kvps, nor a compound, the kind kvp, the artist kvp, where the kind label and the artist label read plain. It names two parts, and a labels row has three, name, value and who wrote this, so kvp says less than the row holds, which tuple did not. Key already means the specialty's keys, the names of the labels a file carries, and the db's row key, its path, so the k in kvp is the third meaning of that letter here. And label is the code's word in the labels table, made_by on a labels row, Labels.ts, label_changes, the labels route and all_labels, which inject-always.sh calls by name, in 32 code files, 85 lines of test_database.py, 41 of the plan, 18 rows of working features, 5 of the lexicon and 4 of add a file, so the rename is a sweep of code, tests, hooks and prose, or the prose says kvp while the code says label. Deciding question: is a name that says the shape and nothing else, three letters spelled out once in the lexicon, worth more than a word that can be said, made plural and put in a compound, and is the code's own? Decided 12 September 2026: label stays, in the shared lexicon.

- 12 September 2026; **rename label to tuple, for a kind or a tag on a file, a name and a value with who wrote it.** For: a tuple is an ordered set of a fixed length, and a labels row is exactly one, a triple of name, value and who wrote this, so the word is truer of the row than pair or key value is. It is a standard technical word every programmer knows, Python's own type, and no meaning of it is a screen's, so it never reads as the text beside a control, which label does. Nothing in ov's code or memory uses it, and the shared truths use it twice, once for python's COLLECTIONS tuple in create a project, its plain code sense, so a tuples table collides with nothing. It fits kind, tag, artist, album and title alike, and a rule gives a tuple, the ai suggests a tuple, and who wrote this is the tuple's third part rather than a thing said about it. Against: a tuple says nothing about a file or a key, only a count, so a tuple on a file says less than a label on a file, and in compounds it reads as code, the kind tuple, the tag tuples, the artist tuple, where a hand reader hears a type, not a thing on a song. A db row is a tuple already, so the files row, the sources row and the rules row are tuples too, and the word names the shape of every row while meaning one table's. It is not everyday English, so a sentence for a person who is not a programmer needs the pair spelled out beside it. And label is the code's word in the labels table, made_by on a labels row, Labels.ts, label_changes, the labels route and all_labels, which inject-always.sh calls by name, in 32 code files, 85 lines of test_database.py, 41 of the plan, 18 rows of working features, 5 of the lexicon and 4 of add a file, so the rename is a sweep of code, tests, hooks and prose together, or the prose says tuple while the code says label. Deciding question: is the thing on a file best named by its shape, a triple, which tuple says exactly and every other row shares, or by its role, a thing someone put on the file, which label says and the screen sense muddies? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **property, or prop, in place of label, for a kind or a tag on a file, a name and a value with who wrote it.** For property: the standard technical word for a name and a value on a thing, an object's property in every language, so a programmer reads the pair at once, and it is the old block's own word, the roadmap saying not in properties in the files, so the rows in the db are the properties that were in the files, moved. It names the pair and fits kind, tag, artist, album and title alike, and who wrote this reads right on it: a property a hand set, a rule gave, the ai suggests. In compounds it reads plain, the kind property, the artist property, the properties table. Nothing in ov's memory, the code or either lexicon uses property for anything of its own. Against property: three syllables against label's two, the longest word yet in a row's name, and the plan's rows say labels 41 times. Against prop: it is taken. A prop is Svelte's word for what a component is handed, $props() in 9 lines of ov's components, adopt kb's four snippet props, and the shared lexicon names it as a thing with a name in the code, so a kind prop reads as a component's argument before it reads as a row. Against both: label is the code's word in the labels table, made_by on a labels row, Labels.ts, label_changes and the routes inject-always.sh calls by name, and every project's word in the shared lexicon, so the code renames with the prose or disagrees with it. Deciding question: is the thing on a file an object's property, the programmer's pair, worth three syllables and the rename, with prop out since Svelte owns it, or is label, two syllables and the code's own, the word that stays? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **fact in place of label, for a kind or a tag on a file, a name and a value with who wrote it.** For: one syllable, plain, and the db's own purpose in a word, since the db holds what the files do not say about themselves. A fact has a source, so who wrote this, hand, rule or ai, completes it: a fact a hand wrote, a fact a rule gave, a fact the ai suggests and nobody has accepted, and a false one is thrown out. It names the pair: the kind fact says kind, howto, the artist fact says artist, and a name. It fits kind, tag, artist, album and title alike, and the four fields on the files row are facts too, the disk's own, as the specialty tables already say, so one word covers every row. And the configuration a host hands kb is facts as well, name and value declared before mounting, so the plan's five uses of the word stay true and the word stretches, not moves. Nothing in the code or either lexicon uses it, so the code's tables rename without a collision, facts for labels, made_by on a facts row. Against: a fact is true, and a tag the ai suggests, a stale kind, or a title a rule got wrong is a fact the file wears that is not so, so the word promises what the rows cannot keep, where label promises only that someone put it there. In a compound it reads oddly, the kind fact, the tag facts, the artist fact, where the kind label and the artist label read plain. One word for the labels rows, the files row's fields and the host's configuration means three things in the plan answer to it, and a sentence must say which. And label is the code's word in the table, the column, Labels.ts, label_changes and the routes inject-always.sh calls by name, 41 lines of the plan and 18 rows of working features, so the code renames with the prose or disagrees with it. Deciding question: is a row the db holds a fact, true by being there, so that a wrong one is a wrong fact and the word holds, or a claim someone made, which who wrote this says and label fits, so that fact promises too much? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **attribute in place of label, for a kind or a tag on a file, the thing being a quality, a measure and a distinction at once.** For: attribute is the one word that carries all three in plain English, a quality a thing has, a measure of it, and what sets it apart, and it is the standard technical word for a named value on a thing, html's attributes and a db's, so a programmer reads it right first time. Its verb is the signature: to attribute a thing to someone is to say who gave it, so who wrote this, hand, rule or ai, reads right on an attribute where it read wrong on trait and aspect, and a rule that gives one or the ai that suggests one reads right too. It names the pair, a name and a value, as label does, and fits kind, tag, artist, album and title alike. Nothing in ov's memory or the shared lexicon uses it for anything of its own. Against: three syllables against label's two, and long in a table cell or a row's name, attributes row against labels row. In the code it already means the html kind, six getAttribute and setAttribute calls in Edit_Markdown.svelte and the style guide's style attributes, so an attribute in a comment can be either until the sentence says. And label is the code's word for the thing itself, the labels table, made_by on a labels row, Labels.ts, label_changes, the labels route and all_labels, which inject-always.sh calls by name, 41 lines of the plan and 18 rows of working features, so the prose says attribute while the code says label, or the code renames with the prose. Deciding question: is the html sense close enough to be the same word, a named value on a thing, so that attribute is one meaning throughout and worth the rename, or a second meaning, so that label, the code's own, stays? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **aspect in place of label, for a kind or a tag on a file.** For: two syllables, plain, and no word of ov's, the shared lexicon's or the code's, save css's aspect-ratio in one pill. An aspect is one way of looking at a thing, which is what the kinds row and the tag areas offer, and a hierarchy by artist or by album is the list seen from one aspect, so the word fits the filters and the hierarchies as well as the rows. Against: an aspect is a side the thing shows, not a thing put on it, so who wrote this, hand, rule or ai, reads wrong on an aspect, as it does on trait, and a rule that gives an aspect or the ai that suggests one reads wrong the same way. An aspect is one of few, the aspects of a thing, where a file's tags are many and a value each, so the artist aspect of a song is the artist, not the row that says artist, and the word names the name half of the pair, as key does. Aspect-oriented is a programmer's term of its own, code woven in across a program, which reads first in a code comment. And label is every project's word in the shared lexicon and the code's word in the table, the column, Labels.ts and the routes. Deciding question: is the word for one way of seeing a file, which aspect names and the hierarchies want, or for one thing someone wrote on it, which label names and who wrote this needs? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **field, the db's word, in place of label, for a kind or a tag on a file.** For: it is the db's own word and one syllable, and a kind is one per file, a hand kind winning over a rule's, so it is a column in all but storage. A reader of the tables section then sees the storage in the word. Against: field already names something else here, exactly: the four columns on the files row, title, description, use_when and date, FIELDS in database.py with set_fields and all_fields, the set-fields route, 16 lines of the dispatcher, 15 of the plan and the four fields' rows in the label form at step 15, so a label called a field makes the four fields ambiguous in every one of those. In the db a field is a column, one value per row and nobody's, and a label is a row of the labels table, many per file, tags and artists among them, each signed with who wrote this, hand, rule or ai, which no column carries. use_when holds several values as csv, and a csv column cannot sign each value. And label is every project's word in the shared lexicon. Deciding question: does the word name the storage, a column on the files row, which field means and the four already are, or the thing on the file with whoever wrote it, which a labels row is, many per file and each signed? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **trait in place of label, for a kind or a tag on a file.** For: one syllable, and in plain English a trait is a quality of a thing, which a kind and a tag are, what sort of file it is and what it is about. Nothing in ov's code, ov's memory or the shared lexicon uses the word, so it collides with nothing here. Against: to a programmer a trait is Rust's, PHP's and Scala's word for shared behavior a type takes on, so in code comments and in Files.ts's 62 label lines, database.py's 31 and the dispatcher's 48 it reads as that first. A trait is a quality the thing has of itself, and a label is a thing someone puts on: who wrote this, hand, rule or ai, sits on a label and reads wrong on a trait, and a rule that gives a trait, or the ai that suggests one, reads wrong the same way. Music's three, artist, album and title, are facts about a file and not qualities of it, so trait fits kind and tag and strains on the three rows the plugin writes. And ji's db spec uses trait already for something of its own. Deciding question: is a kind or a tag a quality the file has, which trait names and who wrote this contradicts, or a thing someone put on the file, which label names and who wrote this completes? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **key in place of label, for a kind or a tag on a file.** For: one syllable, and a label is a pair, a name and a value, which key names in every dictionary since. The word sits in the shared lexicon's own list of words every project says, so a change there reaches every project at once. Against: key already means three things here. The specialty's keys, in what it is and in both specialty definitions, are the names of the labels a file carries, artist, album and title, so key already names the name half of the pair, and using it for the pair muddles the two. The db keys a file's row by its path, keyed in three places of database.py's own words, and File.ts's key_of is a file's collection and path, so key already names what finds a row, not what sits on one. And label is the code's word, the labels table, made_by on a labels row, Labels.ts, label_changes, the /labels route and all_labels, which inject-always.sh calls by name, so prose saying key and code saying label disagree, or the code renames too, 41 lines of the plan, 18 rows of working features and the hooks with it. The shared lexicon's label entry is stale either way: it still says the five lines at the top of a file, and no file has carried them since 10 September. Deciding question: does key name the pair, name and value, which label names today, or the name alone, which the specialty's keys already name? Decided 12 September 2026: label stays, in the shared lexicon.

- 11 September 2026; **put every open question into the plan as a numbered step before the step that needs its answer, or keep the open questions section.** Today the plan is 28 steps, and the open questions section holds ten open: four under can these wait, the title's home, the artist as source or label and the tag reader, all needed by step 6, and the threading decision, needed by 5e; five under kb, album under artist and track order, needed by step 26, and click plays, the steppers playing on and cover art, needed by mu's real player, which no step builds; and three for later, needed by no step. adopt kb's two are needed by step 10. For: step 1 already is a question step, answered before steps 3 to 7, and it worked. With every question in its place, ticking down the list never meets an unanswered question, and the open questions section goes, one place fewer. Against: as steps of their own the numbering runs to about 36 and every step number is renumbered a second time in one day, 8 lines of decisions.md, 2 of adopt kb.md and 2 of index.md. A question step has no proof of its own, only that its checkbox is ticked, so the list mixes decisions and builds. The three for later precede no step, so they stay outside, and the section is not gone. And the player's three questions precede a step the list lacks, mu adopting kb, its bridge, its player as the operation view snippet and its filter sections, the sixth item of the music specialty with no step of its own. A middle way: each question a substep of the step that needs it, numbers kept, the tag reader's three as 6a to 6c before the reading as 6d, adopt kb's two as 10a and 10b, the two hierarchy questions as 26a and 26b, and one new step 29, mu adopts kb, added at the end with the player's three as 29a to 29c, so the list gains the step it lacked and renumbers nothing. The three for later keep their subsection. Deciding question: do the questions become steps of their own, renumbering 28 to about 36 and every reference again, or substeps of the step that needs them, every number kept, with step 29 added for mu adopting kb? Decided 11 September 2026: substeps, 6a to 6c, 10a and 10b, 26a and 26b, 29a to 29c, with step 29, mu adopts kb, added, and the three for later kept under open questions. Then, the same day, the three became steps 30 to 32, vob, the letter hierarchy and rules for the ai, and the open questions section went, with the kb list's sub-bullets that the steps now hold.

- 11 September 2026; **gather every step of zone/music and ai.md's plan into one section, or keep the five lists.** Today the plan section holds five lists, 42 numbered checkboxes: the order's four items, ov strip down's sixteen with 3a to 3e, the four music phases, music first's eight, and the threading proposal's five, and each list names the others by number, step 3 reduced to 3a and 3c, the six fixes being music first's step 5, music phases 2 to 4 done by steps 3 to 8. For gathering: one list, one numbering, one place to tick, and the order section becomes the list itself, so a reader follows the numbers down the page instead of across four lists. Two duplicates go: music phases 2 to 4 say what music first's steps 3 to 8 say, and the threading proposal's five lines are step 5's inside. The strip's step 3 and music first's step 3 both build the collections table, and one list holds it once. Every step already carries its proof and its risk, so the merge changes no content, only numbers and headings. Against: the strip's sixteen are one unit with an intro of their own, the proof rule, the looking-proved risk and the 87 rows, which hold for the strip and not for music first's steps, so one list wears rules that fit half of it. The step numbers are named elsewhere: 7 lines of decisions.md, 2 of adopt kb.md, 1 of index.md and 29 of the file itself, and a running numbering renumbers every one, or a stale number points at the wrong step. The kb list at the top is the what, and the order's item 3, kb's new work, points at it, so gathered steps either say those items again, two places for one thing, or point at the kb list, a fifth list under another name. And music first was decided as a named proposal with its own eight steps, so its name and its decision line point at a list that would no longer exist. A middle way: one `### steps` section holding the four groups as sub-headings, music first, ov strip down, kb's new work and the drop box, each step keeping its own number within its group, the music phases and the threading sections cut as duplicates, the order section reduced to one line naming the groups in order. Deciding question: is the confusion the four lists, cured by one running numbering that renumbers ten references in three other files, or the cross-references and duplicates, cured by the middle way that keeps every number? Decided 11 September 2026: one list, consecutively numbered 1 to 28 under four group headings, music first, ov strip down, kb's new work and the drop box, with substeps where a step has parts, the music phases and the threading section folded in, and every step number in decisions.md, adopt kb.md and index.md renumbered.

- 11 September 2026; **build the six lines of threading the dispatcher now, or leave them in the proposal.** For: the six lines are the same under either order, kb's new work or music first, since both name this fix, so nothing is built twice. A look is 0.04 seconds over 346 files, so a fault shows on a quiet db, where the first long caller, music's scan or the ai call, would hide it under its own stall. sqlite keeps every transaction whole, so a race can fail a request and cannot spoil a row, and the whole change reverts in six lines, WAL by one pragma back. The hooks open the db through database.py as the same user, so WAL changes nothing for them. Against: nothing today asks for it, principle 3, and this is the third pac saying so. The proof is weak: the new check fires two rescans at once, and at 0.04 seconds each they seldom overlap, so a pass says no error was seen, not that the wait was exercised, and a race that a test cannot force is proved only by reading the six lines. WAL puts two files beside the db, so every future saved file made from ov.db, step 2's first, must checkpoint or lose the last writes, a rule for a plan not yet built. And the live dispatcher is the one ov and the hub use, so a slip stalls or fails ov's fetches until a restart, **risk low**, since the db is safe and the revert is six lines. Deciding question: is a fault that a test cannot force, and that reading six lines can, enough of a proof to call the fix done, or does done wait for the first long caller, whose stall the fix exists to remove?

- 11 September 2026; **once the dispatcher is threaded, a mutex around the look, or the watcher as the only thread that looks.** For the mutex: three lines, a threading.Lock at the top and a with block in look and in the rules run, the standard library's, released on an exception too. The five callers keep their code and each gets a whole, current look: /all-labels answers under a file's new path the moment it is asked. RULED and the reconcile are touched by one thread at a time, so the two faults, the dict changed under an iteration and a new row inserted twice, cannot happen. No call inside the look takes the lock again, so a plain Lock does, not a reentrant one. Against the mutex: a look waits behind a look, so whatever holds the lock long, music's first scan over tens of thousands of files or an ai call of seconds per changed file inside the rules run, holds every caller of the look for as long as it runs, which is the one-request-at-a-time server again for those callers. So the mutex comes with a rule: the lock is held for a look of a fraction of a second, and long work, a scan or an ai call, runs in a thread of its own outside it, writing rows through its own connection, which sqlite's 5 second wait already handles. That is the kb list's other half, long work in a thread that answers progress. For the watcher alone: no lock at all, since one thread touches RULED and reconciles, and a request never blocks on a look. Against: /all-labels answers what the last pass left, up to 3 seconds stale, so a file moved a moment ago answers under its old path once. /rescan and a rule added or removed ask the watcher for a pass and wait for it, an Event and a flag, ten lines, not three, and the test suite's in-process calls of the look go through the watcher too. Deciding question: does a caller need its own look the moment it asks, the mutex, or a look within 3 seconds, the watcher alone, given that either way the long work runs outside the look?

- 11 September 2026; **thread the dispatcher now, ThreadingHTTPServer in place of HTTPServer, or with kb, as the performance fix under the kb list.** For: the change is the server's base class on one line and its import, the standard library's own, its request threads daemon so a restart's exit is not held. The db is opened per call, every write sits inside one transaction, and sqlite waits up to 5 seconds for a lock, so the watcher thread already runs beside the server on the same db, and the hub's long routes, rebuild, restart sites and tests, already run in threads of their own. A look takes 0.04 seconds today over 346 files, so the fix is proved on a quiet db before music's tens of thousands make a look long. Against: a look has five callers, the watcher every 3 seconds, /all-labels before it answers, /rescan, and a rule added or removed rerunning everything, and RULED, a plain dict, is read and written by every rules run, so two looks at once can raise dictionary changed size during iteration, and two reconciles at once can insert the same new row twice, the second failing on UNIQUE. So the fix is a lock around the look and the rules run as well, three lines, principle 2's minimum being more than one. The suite starts a plain HTTPServer of its own, so a threading fault shows only once the test starts the threaded one and fires two looks at once. And nothing today asks for it: 0.04 seconds stalls nobody, so under principle 3 the stall the ai call would bring is the ai's cost, not the dispatcher's. Deciding question: is the lock around the look worth writing before anything holds the server longer than 0.04 seconds, or with the first thing that does, the ai call or music's scan?

- 11 September 2026; **the proposed rules: the ai as a third writer of labels, instructed by a markdown file beside each plugin and run inside the rules pass, or no ai writer, the composing and the pattern rules alone.** For: the db already keeps ai among who wrote this, ov already passes over ai rows until one is accepted, the rules pass already runs on changed files alone, and ji already makes the chat call, so the new code is the instructions file's read, one call, the settings file and the editor's accepting, principle 2's minimum. A rule never changes or removes a label a person put on, and the proposal keeps that: no hand row and no rule row is touched. The composing reads a bare file's first heading and first sentence and gives a fallback kind, where the ai reads the whole text, so a bare file gets a better first answer. Off, it costs nothing. Against: today the db holds 346 files, 13 with no kind and 0 ai rows, so the bare case is 13 files, and the composing already answers it the first time each is opened, which makes the third writer speculative under principle 2 until a count says otherwise. The dispatcher answers one request at a time, so every call, seconds each, stalls the hub and ov, the hooks reading the db file and not the server, until the threading fix under the kb list is built. The base url, the key and the workspace name would live in two places, ji's preferences and the dispatcher's settings file, two files claiming one topic. The lexicon has no entry for the ai as a writer, and the word collides with ai the host, so every sentence about the ai in the ai host reads two ways. And music gets nothing until a song takes tags, so the common mechanism is built for one specialty and meant for two, strip step 3's medium risk again. Deciding question: are 13 bare files of 346, each composed the first time it is opened, worth one call per changed file and a second meaning for ai now, or after kb exists and the threading fix is in?

- 11 September 2026; **how a specialty's plugin labels a file: a fourth maker, plugin, or writing as rule inside the rules pass.** For a fourth maker: the rules pass touches rule rows alone, so a plugin's labels survive every look. A label says truly who gave it, and the app can rank hand, plugin, rule. Against: the labels table's check on made_by changes, the real db migrates, and every reader of made_by learns a fourth word. For writing as rule: no schema change, and one pass fills a file's labels. Against: a plugin's label and a rule's are one kind of row, so nobody can tell which gave it. Taking a rule away reruns every plugin over every file, which for music reads every file's tags again. And the rules pass, which reads a name, a location or a text, has to call each specialty's plugin, so the rules engine learns about plugins. Deciding question: does a label have to say whether a plugin or a rule gave it? Decided 11 September 2026: no. A plugin writes as rule, inside the rules pass.

- 11 September 2026; **how a host hands kb its specialty: a configuration set before mount, gallery's way, or snippets at render, panel's way.** For a configuration: one object, set once, read lazily by every kb module, managers included, and the keys and hierarchies are needed before anything mounts. A component is a value, so the filter section, the details section and the operation view ride in the same object. Against: a component in a plain object is outside Svelte's snippet typing, and kb draws panel with snippets while taking its own specialty as configuration, two ways in one app. For snippets: typed, Svelte's own, and what panel already does. Against: keys and hierarchies are not snippets, kb's code outside components cannot read one, so the host hands some things as snippets and the rest as configuration anyway. Deciding question: is the specialty one value, or two: the facts about it, its collection, keys and hierarchies, set on kb's configuration before mount, and the pieces it draws, its filter section, details section and operation view, handed in as snippets? Decided 11 September 2026: two. The facts as configuration, the drawing as snippets, since kb already does both and each part goes where it is read.

- 31 August 2026; **adopt every file in core/ts/common — Debug.ts and Dirty.ts remain (Extensions, Constants, Configuration, and Colors are adopted); each adoption adds its name to the roll at the bottom of core/truth/'adopting core.md'.** For: it finishes common — no copies left, and Dirty is the live danger: ov's copy is core's verbatim, both hold stale_writable stores, so ov code and core components read two different instances of what should be one state — the Extensions crash's silent sibling, worse for making no sound. Debug identical, drift-only. The lesson is already law in the journey file: an adoption is never a copy. And the roll makes the journey file a running record, not a snapshot — the next host watches adoption happen. Against: 27 files import the two copies — the same sweep as every adoption, but stacked on wiring the Mac has not yet proven (yarn dev owes the vite half, vitest owes the tests); each further adoption piles unverified change on unverified change. And Dirty is stateful — module stores move from ov's copy into core's module, bending "state lives in the host" a little further than Colors already bent it. Deciding question: both now, or Dirty first — the silent divergence — with Debug waiting until the Mac proves the alias?

- 31 August 2026; **the shifted-titles bug: key the stack's sections by identity, or make the separator re-adopt its pills.** For keying (name each section, `{#each ... (section.name)}`): the list is the thing that shifts, so the list is where identity belongs; Svelte then moves each section's whole slot intact when back links comes and goes, separators riding along. Small, declarative, no imperative DOM work. Against keying: every Stack caller must name its sections — a new required field across the app. Adoption still happens once, so the fragility is not cured, only dodged: an action whose element changes in place still shows the stale pill. And a keyed section that leaves the array destroys its wrapper with the adopted pill inside — if the section returns, the pill must survive that. For re-adoption (the separator watches its actions and takes the current elements whenever they change): one edit in one file heals every stack everywhere, this shift included and the next one; identity stops mattering, since each slot always wears what its current section hands it. Against re-adoption: adoption is imperative DOM-moving, and doing it inside an effect invites loops and fights with Svelte's own patching; during a shift one pill can be claimed by two separators, so ordering must be right; harder to reason about than a key. Deciding question: whose bug is it — the list that shifted (key it), or the one-time adoption (make it live)? Re-adoption fixes the class; keying fixes this instance and leaves one-time adoption waiting to bite again. Decided 31 August 2026: neither — the gate that hid back links owns the bug. The section never leaves the list; it hides in place (a stacked section's new hidden state: no line, no height, no fold to bring back), so nothing below it shifts.

- 30 August 2026; **merge all small ts files into one common/core.ts** — small meaning at most the lines in Configuration.ts plus 3, today 2 + 3 = 5. For: the seam gathered into one file — every borrowing from core is one export line in a file whose name says what it is, the one-place readability the lib/core folder wanted, without a folder and without colliding with the alias's name. Three one-liners become one; a future adoption adds a line rather than a file. Against: with the threshold at 5 lines, four files qualify — the three borrowings (Constants 4, Colors 4, Configuration 2) and one stray: Types.ts at 5, ov's own. So even a sharp threshold mixes one concept in that is not a borrowing, and one-concept-one-file is the design's own rule; the sizing table caps a file's length, never the count of files. The criterion also moves as Configuration grows. And the sweep is the same one the lib/core pac choked on: every importer of the merged files changes its path. The standing rival: alias-direct — importers write `from 'core/ts/...'`, no shim files at all, the borrowing readable in every import line; same sweep, zero files. Deciding question: is the merge criterion *small* or *borrowed from core*? Borrowed-from-core makes core.ts an honest seam file whatever its length; small makes it a drawer, and the name stops being true at the first stray. Decided 30 August 2026: borrowed. common/Core.ts holds ov's borrowings from core — Constants, Configuration, Colors, and Extensions (a side-effect import) — one line each; Types.ts, ov's own, keeps its own file.

- 27 August 2026; **move handoff.md and code debt.md into the new design as they are.**

  For: both are live — pull-don't-push says move a thing the day work reaches for it, and today's session reached for both. Moving them untouched is the cheapest possible move: no editing, no judgment, nothing lost by trimming wrongly. ov's memory has nowhere to say what is owed — `decisions.md` holds rationales, `controls.md` holds design — and that missing home is exactly why handoff keeps getting written. And the ratchet is already being broken while they stay outside: this session wrote a summary into handoff.md today, into a file on the death list.

  Against: "as is" imports history, which the migration rules forbid (changed to inception, on 28 August 2026). `code debt.md` is 390 lines, of which 315 are its done section — 292 finished checkboxes against 27 unchecked. Git already holds that work and the log holds what is recent. Size: at 390 lines it breaks the ~100-line truth limit four times over, and any `use_when` broad enough to be useful would eat the orientation budget. Duplication: handoff's "where it stands" and `index.md`'s current-state paragraph would both claim one topic, as would handoff's open items and the log's `Q:` lines — two files claiming one topic is the bug the design names. And handoff is a journal by nature: dated, narrative, rewritten each session. That is what `log.md` is; a truth file rewritten wholesale every session is a log wearing the wrong name.

  A middle path, if wanted: `code debt.md` becomes `truth/debt.md` holding only the 27 unchecked items and its done section stays behind, abandoned in place; handoff stops being a file at all — its "what to do next" becomes the current-state paragraph in `index.md` plus `Q:` lines in the log.

  Deciding question: is "as is" worth importing 292 finished checkboxes, or is the point of the move that these two files finally stop being a second system?
