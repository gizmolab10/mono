---
description: mj — brought up with boilerplate only; what it is for is not written yet.
---
# mj

Brought up 1 September 2026. What mj is for is not written yet.

**Current state:** three files carry the whole app — `App.svelte`, which draws one line; `Main.ts`, which mounts it and pushes core's sizes onto the page; and `Customizations.ts`, empty, waiting for the first switch that is mj's alone. Everything it takes from core arrives through `common/Core.ts`, and only that file reaches through the alias. Port 5187, registered in the hub under the letter K, in mono's workspaces and in ov's T_Bundle, and in the servers script so it restarts with the others. Checks clean at 406 files.

## Truths

None yet.
