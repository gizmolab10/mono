# Drive

**Current state:** since 9 September 2026 lv imports gallery — the page, the sidebar, the renderer, the galleries, the editing, the router, the parser, what the browser remembers, and both stylesheets — through the `gallery` alias and `common/Gallery.ts`, the way it imports core through `common/Core.ts`. Since 10 September 2026 that page is panel's: a controls row with the hamburger, the site's name and the edit button, a details column for the sidebar, the operation view holding the md file, and a status line while a link leads nowhere. lv's build carries the panel alias for it, while lv's code names only core and gallery. lv's own code is App.svelte, Main.ts, the two bridges and `Customizations.ts`, which holds the four switches gallery reads: the name and the home page are both Little Cloud Vineyard, remembered values are saved under `lv.`, the sidebar is off, so the hamburger shows no column yet. Main.ts hands them to gallery before anything mounts. The plugins run from gallery's folder, named in vite.config.ts. The four netlify functions are lv's own and import stamp and Order from gallery. Its pages, pictures and icon are its own. Check clean at 398 files, 4 tests, build 386 modules. Open: which remote hosted storage, if the pictures outgrow the repo.
