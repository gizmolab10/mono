---
type: reference
title: Migration
description: The death list — what still lives only in the old system. Empty means the old system can be deleted.
tags: [migration, old-system]
use_when: [reaching into notes/guides or notes/work, settling, deciding where an old file's content belongs]
updated: 2026-08-22
---
# Migration

What still lives only in the old system, one line each. Remove a line when its content moves into `memory/` or is declared dead. Empty file = migration complete: delete this file, shrink CLAUDE.md's "Reading on load" to `start` alone.

- `notes/guides/pre-flight/` — always, response, agency, keywords, gates, kinds.of.tasks, pitfalls, lexicon (mono-wide terms)
- `notes/guides/collaborate/` — chat roles, voice, workflow, exclude, gating
- `notes/work/learn.md` — cross-project lessons (the rules they taught, not the stories)
- Per-project `notes/work/` live files — handoff, current context, code debt, work journal, learn
- Per-project file maps (each project's CLAUDE.md names one)
- `ov/notes/guides/pre-flight/lexicon.md` — ov's terms (memory/ov/truth/lexicon.md points there)
- lv `CLAUDE.md` Words section — lv's terms (memory/lv/truth/lexicon.md points there)
- `CLAUDE.md` "Reading on load" list itself — shrinks as the lines above die

Stays by design (not on the death list): `notes/guides/pre-flight/shorthand.md`, the trigger surface.
