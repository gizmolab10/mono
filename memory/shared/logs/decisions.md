# Decisions

## Decisions made during 2026

- 23 September 2026, **T_Position.align puts a placed thing's center on the window's center**, the separator measuring its own left edge the way the file section does for the name; the ancestry is placed that way, so it sits under the name; built the same day, measured headless at 1400px and 1000px, the ancestry's center, the name's and the window's one number
- 23 September 2026, **the file name in the controls row is centered on the window's width**, not on its own row's leftover; built the same day, measured headless at 1400px and 1000px, the name's center at the window's
- 23 September 2026, **T_Position gains align**: a thing placed with it has its center on the file name's center, the name in the controls row above; right, added to Action the same day, is gone, a constant drifting with the window and the file; aborted the same hour, before any build; T_Position.align is in the enum with no reader
- 23 September 2026, **the ancestry text is a label on the separator at the top of the stack below the controls**, centered, handed as an Action with a span, the way the starved no-options span is; Action gains one property, top; the two drive proposals, extend below and its own section, passed over; built the same day on the editor's less-or-more line, at the right, top -8 after -3, transparent, Jonathan's rewrite of the drive and his eye
- 22 September 2026, **a row of saves.jsonl says its size before its division**: timestamp, kb, division, saved; today division comes before kb; the change waits for go
- 22 September 2026, **the resume-browsing button is a fat triangle pointing left, with the same cross**, in place of the circle with a cross, the back button in kb's Controls.svelte; built the same day, seen headless
- 22 September 2026, **ga and kb join T_Bundle**, kb's list of projects, so memory/ga and memory/kb answer to their own projects in the browse filters rather than to memory; built the same day, ga added to the dispatcher's list in ai/plugin.py beside kb, which was there
- 22 September 2026, **the folders seg control drops the grandchildren when it would not fit the width available**: with children and grandchildren together too wide for the row, the grandchildren leave and the children alone are offered; built the same day, the width measured on screen and again when the set or the window changes, a pick that leaves the set let go
- 22 September 2026, **the browse filters gain a folders section**, between projects and kinds, its own line, fold and clear, and the folders seg control moves there out of the kinds section; built the same day
- 22 September 2026, **the folders seg control offers only the children and grandchildren of a memory project's folder**, memory/X/child and memory/X/child/grandchild by name, no deeper; built the same day, a file deeper than a grandchild answering to its nearest folder within the depth
- 22 September 2026, **the rotation's seven pieces are built**, before the week of counts: eight rows after the erase showed three sizes only, so the wait would have measured nothing new; the largest piece with Always is 7.5KB, under the 8.4KB seen shown whole
- 22 September 2026, **CLAUDE.md's text is the truth onload.md**, in shared's truth, imported by CLAUDE.md's one `@` line, with a plain line naming the file; named on load.md for an hour, until /context in a new session showed the import does not take a path with a space; not session, not beginning a chat
- 22 September 2026, **Always is four rules**, Brief, Guess, Withhold the evidence, Mechanism not story, 1.2KB, inside the 2KB co reads on a saved turn: Standard technical English and Name both sides are Response 13 and 14, Interpret first is in agency 7, A reply exists is agency 24, the collision rule is in Conduct, huh? is a shorthand row; not done: Always 2 and agency 7 say the same thing with two labels, I AM GUESSING and unread:; making them one label changes agency's words, which are Jonathan's; he removed agency 7 the same day, so Always 2 alone says it, and agency has 24 rules
- 21 September 2026, **every file named drive.md wears the tag drive**, the 14 in the projects' zone folders, through the dispatcher's add-label route; built the same day
- 21 September 2026, **the tags park and drive join the tag area progress** in ai's closed tag list, beside proposal and journal: drive for the file that is the drive, park for a drive moved whole to a zone file of its own; built the same day
- 21 September 2026, **parked is a state of the drive**: `park` moves the drive whole to a zone file named after its first H2 and erases drive.md, and `drive <X>` brings it back; workflow.md's drive row says so
- 21 September 2026, **the hook's rotation is seven pieces**, each under 6.9K, cut at headings, and **the saves are counted**: each time the Claude harness saves a hook's output to a file, a hook records which division and how large, and every tenth count co reports which division and how often in the chat
- 21 September 2026, **the rule drawn from a markdown hr is 2px thick** on ai's page, where today it is the faint thickness
- 21 September 2026, **every t leaves a lesson in zone/learn.md**: one checkbox line, the murky words, the plain words, and what to say next time; the shorthand's t row and Response 7 say so
- 21 September 2026, **a wikilink turned into html keeps the whitespace before it**; today the conversion consumes it
- 21 September 2026, **do reads: action co must perform**, in the shared lexicon and drive.md; the wordings when the situation needs it and to unblock work weighed and passed over
- 21 September 2026, **do is the fifth word**: the act a rule tells co to take when its case comes up, not a condition; the label before it in pitfalls, Required today, becomes Do. In the shared lexicon beside the four
- 21 September 2026, **the four words are in the shared lexicon in Jonathan's meanings**: check, co looks and compares; required, a feature, library or facility that blocks work when missing; gate, a statement that must be true before the work; detect, a check that runs by itself, every hook detects. The drive rewrites each wrong use, v per file at first
- 20 September 2026, **the tag edit joins the tagset fix** in ai's closed tag list, 40 tags: a file whose prose needs work, beside debug, faster and test
- 19 September 2026, **"huh?" suspends the rules**, Always rule 10: the reply is rewritten under Brief, Withhold the evidence, Mechanism not story and the banned words only; section 10's rule 4, ignore ALL the guide rules, gone
- 19 September 2026, **zone/drafted guide rules.md deleted**, its decided part in workflow.md, its table of the 23 drafts and its breakdown a dated work journal entry, the drafts' fate a question in zone/questions.md
- 19 September 2026, **cadence.md merged into workflow.md**, one file for how work moves, one picture beside it, workflow.svg; workflow's own short Cadence section gone as a twin, cadence.md deleted through the dispatcher
- 19 September 2026, **cadence is one file and one picture**: cadence.md opens with Jonathan's words, back and forth, each receives and then gives, walks one turn command by command, says what each of us wants, and holds the cycle, the any-time tasks and the items with their states, moved from workflow; cadence.svg draws the eight steps as a spine with the commands on top, co's acts beneath and the items below; workflow.svg folded into it
- 19 September 2026, **markdown-check.sh, an after-edit hook for the form of markdown**, the bare placeholder check its first, moved out of plain-english-check.sh, which is for words and names; wired beside it in the settings, with its own test
- 19 September 2026, **a placeholder such as `<X>` is always inside backticks**, since markdown reads a bare one as a tag and the page drops it: the correction captured in zone/learn.md, eight bare ones in four files given backticks, and plain-english-check.sh checks for it. The hook has a flag, PLAIN_ENGLISH_CHECK, false as an experiment: false, it does nothing
- 19 September 2026, **the shorthand `full log`** re-assembles a project's log.md from every committed version in git, every line under its day once, the marker kept; shared's was rebuilt that way the same day, 237 lines
- 19 September 2026, **a draft guide rule starts with Jonathan's correction**, at v or in any turn; learn captures it. The svg's entry arrow and cadence.md's row say so, where they said learn alone
- 18 September 2026, **v enters cadence**: cadence.md has a built work row, built, approved or sent back, and a sentence under the rhythm saying v comes after co acts and before record; cadence.svg has a built work lane after the turn-taking, approved on good, perfect or done, sent back to go on anything else
- 18 September 2026, **past, present and concrete**: logs hold the past, zone the present, truth the concrete. decisions.md moved from truth to logs in ai, core, lv, panel and shared, its open pacs to each project's zone/proposals.md as sections and the decided ones kept; questions.md to the zone in nine projects, proposals.md in four, learn.md in three; working features stays in truth. The indexes, the handbook, workflow, the shorthand, the lexicon, cadence.md and cadence.svg, the CLAUDE files, the three tools and their tests say the new folders
- 18 September 2026, **the logs only grow**: consolidate settles each line and moves the marker, deleting nothing; a final decision stays in decisions.md, dated; the caps on log.md and decisions.md are gone; an answered question, a culled idea and a culled proposal each leave one dated log line; record adds a row to logs/distilled.md when it places a correction, so distilled.md stays and the journal's copy of the table went. Jonathan's goal: the logs remember every twist and turn, so "when did we decide that" is answered from them, never from git. The pac on deleting distilled.md, decided by this
- 18 September 2026, **corrections.md merged into learn.md, a file of checkboxes alone**: a lesson is a checkbox from the start, captured by learn, rewritten by Jonathan's tick, placed by record, and distill is gone; learn.md's old Process and Distilled table are in the journal; di's 17 numbered entries are checkboxes; workflow, the shorthand, the lexicon, the index, cadence.md, cadence.svg, CLAUDE.md, code_debt.py and its truth say learn.md. The two pacs of the same day, each way round, decided by this
- 18 September 2026, **`drive` or `go drive` implements drive.md**, then moves the proposal to working features rewritten as a feature description when it is an app feature, or else to the work journal as what was changed and why. Drive's states are present, implemented, a feature or journaled, in cadence.svg, cadence.md, workflow's cadence and drive rows, the handbook's drive line, the lexicon and a shorthand row. Dissolving into truth on instruction is gone
- 18 September 2026, **shared's proposals.md lives in its zone**, moved from logs through the dispatcher, its row following. The handbook, workflow and the lexicon still say logs/proposals.md for every project, and the three tools find the file in any folder
- 18 September 2026, **a proposal's gone state is culled**, the word ideas already use, not dead: cadence.svg and cadence.md, workflow's cadence and proposal rows, the handbook's and the index's proposals.md lines, the finished and code debt truths, the three tools that count settled proposals and their tests
- 18 September 2026; **cadence.md matches cadence.svg**: a table of what cadence moves, one row per lane, and the drafted guide rules proposal's section merged in as Draft guide rules; the stale section on co's own memory folder replaced by it
- 13 September 2026; **big picture is named unfinished**, the word saying what the file holds: `memory/shared/zone/unfinished.md`, the shorthand `unfinished`, `tools/unfinished.py` and its test, and the truth `truth/unfinished.md`
- 13 September 2026; **every project's log.md lives in its logs folder**, `memory/<project>/logs/log.md`, beside the app's own logs, moved that day for all 14. The big picture walks logs folders for it, its test puts the log there, and create a project's skeleton says so
- 13 September 2026; **standard technical English first, everyday English second**, Always rule 2 in conventions.md: the term any programmer knows, when one exists, over a plain-English paraphrase of it, since the paraphrase made cumbersome metaphors and convoluted clauses. Jonathan's suggestion, after two replies he could not follow
- 7 September 2026; **`truth/` may hold sub-folders.** The rationale's "truth/ never grows subfolders" is removed; a folder inside truth names a design's parts, and a file's kind stays its `type` label.
- 7 September 2026; **every working features file and every pre-flight file is in truth/** — always, response and the banned words folded into shared's conventions.md, the mono-wide lexicon into shared's lexicon.md; the rest moved whole. Fold chosen over side by side.
- 27 August 2026; the set of skills is named the **toolkit** — replacing 'control surface' and the sizing table's stray 'verbs'; the members stay **skills**.
- 25 August 2026; **pac** responses live in the most relevant project's decisions.md, reporting the decision each weighs. when its deciding question is answered, it is rewritten in one short statement about the decision made.
- 25 August 2026; this file holds 2 -> evaluated-undecided and reasoned-decided.
- 25 August 2026; **half** is removed from shorthand and the toolkit — it does not work: the brief versions were mostly inscrutable.
- 25 August 2026; Current guide and work files are NOT yet going to be moved into **archive**/ — they remain in place, as the migration rules already say (changed to inception, on 28 August 2026); archive/ is for what retires inside the memory system.

## Evaluations (pac) decided during 2026

Open pacs live in zone/proposals.md, one section each, since 18 September 2026. A pac decided there comes here as its dated line.

- 22 September 2026 — **CLAUDE.md's contents to a truth, imported by CLAUDE.md.** **pac** 22 September 2026

**What exists**: `CLAUDE.md` at the repo's top, 54 lines: the project list, the principles, who, reading on load, the canary and the defaults. Claude Code reads it at the start of every session and again when it changes; a subfolder's CLAUDE file loads only when co reads files in that folder; a bare line `@path`, outside backticks, imports that file into CLAUDE.md, the path relative to CLAUDE.md, four hops deep. Verified in the Claude Code docs, 22 September 2026. No CLAUDE file in the repo imports today. The truths CLAUDE.md points at already sit in `memory/shared/truth`: [[conventions]], [[agency]], the lexicon, the shorthand, keywords, gates, kinds of tasks, pitfalls. The `start` shorthand names a Session protocol section of the handbook that is not there. [[system failure]]'s proposal to rewrite CLAUDE.md splits it into Knowledge and Rules under a switch line at its top.

**For**: the text becomes a truth like the others: in the memory system, in the index and the map, tagged, opened in the app, listed by the dispatcher, counted by the tools. CLAUDE.md is one line, `@memory/shared/truth/session.md`, and the Claude harness loads the truth at session start and on change; nothing depends on co following an instruction to read it. The system failure rewrite then edits a truth, not a file outside memory.

**Against**: the name says when the file is read, not what it holds, and it sits beside the handbook's dead Session protocol and ai's tag session. The switch proposal wants CLAUDE.md to open with a line that reads `disableAllHooks` before anything else; an import line cannot branch, so the switch line stays in CLAUDE.md above the import, or the whole switch moves into the truth. Reading on load lists files to read; moved into an imported truth it reads the same, nothing gained there. Cost, counted: one new truth, one line in CLAUDE.md, the index and the map, and the shorthand's `claude` row, which says reread CLAUDE.md.

**Deciding question**: is CLAUDE.md's text a truth, to live with the others under a name that says what it holds, or the Claude harness's own entry point, to stay where the harness reads it and import nothing? Decided 22 September 2026: yes; the truth is onload.md, and CLAUDE.md holds one `@` line and one plain line naming it
- 22 September 2026 — **the name beginning a chat.** **pac** 22 September 2026, on the name for the truth in the pac above; co had said on load.md

**What exists**: the truth would hold CLAUDE.md's text: the project list, the principles, who, reading on load, the canary and the defaults, yarn, no preview, tone, docs. Response 11 says a file's name says what the reader does with it. The lexicon has session, one conversation start to end, and turn; it has no entry for chat, the word the shorthand's `summary` and `murky` rows use. Two truths are already named for what the reader does: `create a project.md` and `keep shop.md`. Claude Code's own word is session, as in its SessionStart hook.

**For**: it says what the reader does with the file, begin a chat, the way create a project and keep shop do; three plain words Jonathan uses; no collision with ai's tag session, the shorthand `start`, or the handbook's dead Session protocol.

**Against**: the file holds more than the beginning: the principles, who and the defaults hold for the whole chat, and a reader looking for never npm or no preview would not open beginning a chat. chat is not in the lexicon where session is, so the name uses the word the system does not define for the thing it defines as session. A name that reads as a howto, do this to begin, on a file whose kind is rules.

**Deciding question**: is the file what co does when a chat begins, read once and set aside, or the rules that hold for the whole chat, which the name would understate? Decided 22 September 2026: no; on load, CLAUDE.md's own heading for the part that matters, spelled onload.md since the import stops at a space
- 21 September 2026 — **the hook's rotation from three pieces to seven.** **pac** 21 September 2026

**What exists**: on every turn co reads Always, 3.1K, and one of three pieces in turn: the rest of conventions.md, 18.7K, agency.md, 5.3K, the lexicon, 13.6K. When Always and the piece pass about 10K, co's guess, the Claude harness saves the whole to a file and co reads its first 2KB, rules 1 to 5 of Always. So two turns in three co reads nothing past Always, and the rest of conventions.md reaches co on no turn at all. The seven pieces of the proposal in zone/drive.md are each under 6.9K.

**For**: every piece is read whole, every turn, so every rule reaches co again; the rest of conventions.md, with Response and its rule 4 on file names, is read for the first time since the hook was written; a rule sits in the same piece each time, so a reader knows when it comes.

**Against**: a full round takes seven turns where it took three, so any one rule comes round less than half as often, agency every seventh turn instead of every third; the pieces cut across files, a lexicon section beside Conduct, so what co reads on a turn is no one truth; the cut points are headings and change when a file is edited, so the hook cuts by reading, and a heading added between two pieces moves every piece after it; and the 10K is a guess, so the pieces may be smaller than they need to be, or one may still be saved.

**Deciding question**: is reading every rule whole once in seven turns better than reading a third of them whole every third turn and the rest never, and does the 10K get measured before the cut? Decided: seven, and the saves measured first: a hook counts each time the Claude harness saves a hook's output to a file, with the division that caused it, and reports which and how often in the chat on every tenth count.
- 21 September 2026 — **the wording of do: action co must perform.** **pac** 21 September 2026

**What exists**: the third wording of the morning, after when the situation needs it and to unblock work, both weighed below. Four words, no condition.

**For**: nothing in it can be wrong about when, since it says nothing about when; the pitfall the label sits in says when. Must carries the rule: an obligation is what a rule gives. It fits all five pitfall lines, the ones that stop as well as the ones that go on. The four other words are conditions, and action is the one word that sets do apart from them, which is the entry's job.

**Against**: every agency rule is an action co must perform, so the entry alone does not say which action do is; only the label's place, after Anti-pattern, does. Perform is do's twin at line 84 of the lexicon.

**Deciding question**: is the entry's job to say what kind of thing do is, an action against four conditions, or also when it applies, which needs the rule in the sentence? Decided: this one, 21 September 2026.
- 21 September 2026 — **the wording of do: action co must perform to unblock work.** **pac** 21 September 2026

**What exists**: the second wording Jonathan offered the same morning, after action co must perform when the situation needs it, weighed in the pac below. The lexicon's required is a feature, library or facility that blocks work if it is missing, and its gate is what must be true before the work.

**For**: it joins the family: required and gate name what blocks work, do names what removes the block, and the five words then read as one idea, work and what holds it. Unblock is a word every programmer knows. Six words.

**Against**: two of the five pitfall lines it would label stop work rather than unblock it: pitfall 19, stop, produce the analysis, wait for a decision, and pitfall 16, on rejection stop and ask. The lines say the right act whether or not work is blocked, so unblock names three of five. And the rule is still gone from the sentence, the open point of the pac below.

**Deciding question**: is do the action that removes a block on work, or the right action a rule names, blocked or not? Decided: not this one; action co must perform, 21 September 2026.
- 21 September 2026 — **the wording of do: action co must perform when the situation needs it.** **pac** 21 September 2026

**What exists**: the lexicon's entry, co's words: the act a rule tells co to take when the situation the rule describes happens. Jonathan's: action co must perform when the situation needs it. Line 84 of the lexicon already pairs do with perform under Verbs to use carefully.

**For**: nine words against fifteen. Must perform says the obligation outright, where tells co to take only implies it. Needs it is plain.

**Against**: the rule is gone from the sentence, and without it do names any action a situation needs, which is every action co takes; the five pitfall lines are what a rule prescribes, and that is what makes Do a label beside Anti-pattern. Who says the situation needs it is left open, where the rule says it. Perform is do's twin at line 84, so the entry defines do with do.

**Deciding question**: does do name any action a situation needs, or the action a rule prescribes for a situation? Decided: not this one; action co must perform, 21 September 2026.
- 21 September 2026 — **remedy as the fifth word.** **pac** 21 September 2026

**What exists**: the fifth word of the drive names the act a rule tells co to take when its case comes up, the line after Anti-pattern in five pitfalls, labeled Required today. do was decided this morning and is in the lexicon; Jonathan wrote fix into drive.md after, and fix already means the shorthand command and ai's tagset. remedy is in no truth, no lexicon, no tagset and no code.

**For**: one meaning, nothing to collide with. The pair Anti-pattern and Remedy reads as the fault and its cure, which is what the five pitfalls are. A noun, so the label reads as a heading beside Anti-pattern, where Do reads as an order.

**Against**: a rarer word than do, and Always 2 puts the everyday word first. It says a fault came first, while some of the five lines are the normal case, pitfall 19's stop and produce the analysis, with no fault yet. Two syllables against one, and the lexicon entry written this morning says do.

**Deciding question**: is the fifth word the cure for the anti-pattern above it, remedy, or the act in the normal case, do? Decided: do, the everyday word, fitting the normal case as well as the cure.
- 21 September 2026 — **the four words: co rewrites each wrong use without waiting for a decision.** **pac** 20 September 2026

**What exists**: the drive's plan in zone/drive.md, section 1: for every use of check, required, gate and detect in the shared truths, CLAUDE.md and the hooks, co reads the sentence around it, and where the word does not fit its meaning, replaces it. The counts of 19 September: check in 51 files, require in 15, gate in 9, detect in 2. Agency 8 says co changes nothing until Jonathan says go; agency 20 says guide prose co writes is a draft he rewrites within minutes.

**For**: the four meanings are Jonathan's, so each call is a reading against his words, not co's taste; a decision on every use would be about a hundred turns; one D: line per file and git make every replacement reversible; v after each file catches a wrong call while it is one file old.

**Against**: words are co's biggest fault, 79 of 136 corrections in this session's transcript, and a wrong word in a truth is the very fault the drive is fixing; agency 20 means Jonathan reads every rewrite anyway, after instead of before; a blanket go on the plan is a go on a hundred edits Jonathan has not seen.

**Deciding question**: is the go on the plan a go on every replacement, with v after each file, or does each replacement need Jonathan's eye before it is written? Decided: go, with Jonathan's v after each file at first.
- 20 September 2026 — **add a tag edit to the tagset fix.** **pac** 20 September 2026

**What exists**: ai's tags are a closed, alphabetized list of 39 in `ai/src/lib/ts/common/Customizations.ts`, gathered into ten tagsets, every tag in exactly one, the tag areas test proving the two lists agree. fix holds debug, faster and test: a fault to find, speed to gain, proof to write, each the work a piece of code needs. No file in ai's db wears a tag named edit. In kb's lexicon edit is the app's own word for the editor view, and ai's design folder holds editing.md.

**For**: a file whose prose needs work has no tag today; stale, in the fate tagset, says out of date, not needs rewriting. edit names the work the file needs, the way debug names the work code needs, so fix is its tagset, and the 62 truths over 100 lines and the 23 drafts would wear it. One line in the tag list, one in the tagset, the test's count.

**Against**: fix's three tags are about code and edit is about prose, so the tagset's meaning widens. edit is already the editor's name in kb, so a tag edit reads as opened in the editor, or about the editor, which is a subject and would sit in ux. Who puts it on and takes it off is unsaid: 62 files by hand today, and nothing removes it when the file is shortened.

**Deciding question**: does edit mean the file needs its prose worked, which is fix, or is about the editor, which is ux, and if the first, does it come off by hand or when the file drops under the cap? Decided: added, edit meaning the file's prose needs work.
- 19 September 2026 — **consolidate the subsection on sorting the 142 guides**, culled the day it was written: Jonathan answered the subsection's two questions, YES to merging and YES to shortening each guide as it is moved, and made it the drive.
- 19 September 2026 — **sort the 142 guides now sitting unsorted in truth/**, the pac of 7 September, decided: both open questions YES, each guide merged into the truth that already covers its topic, or shortened, at the moment it is moved into truth/. Made the drive the same day.
- 19 September 2026 — **allow "huh?" to override Always.** What exists: conventions.md section 10, rewritten by Jonathan today, rule 4: on "huh?", ignore ALL the guide rules and rewrite the response. Always says its nine rules govern every reply and are the highest priority, and its rule 9 says when two rules collide one must go. No shorthand row and no lexicon entry define "huh?"; section 10 is the one place it is written. For: "huh?" means the reply failed Jonathan as a reader, so the rules that shaped it are the suspects, and a rewrite under the same rules gives the same reply; the override names the one case, and Always keeps the rest. Against: ALL includes Brief, the banned words, no story and no evidence, so the rewrite can be long, told as a story and wear a banned word, which "huh?" never asked for; Always says highest priority, so a rule below it that overrides it makes Always's own claim false unless Always says so too, in one line; which rule the reply broke stays unknown, the override turning every rule off where one was at fault. Decided: Always rule 10, "huh?" suspends every rule but Brief, Withhold the evidence, Mechanism not story and the banned words; section 10's rule 4 gone.
- 19 September 2026 — **delete zone/drafted guide rules.md.** What exists: the file, proposed 15 September, decided 18 September: its section is built, the Draft guide rules section of workflow.md since cadence.md merged in today. It still holds the section as first written, naming corrections.md, a file merged into learn.md; three of its four other changes, marked not done: the lexicon's guide rule and draft guide rule, the 23 drafts as checkbox lines, and the debt check; the table of the 23 drafts of 15 September with where each sits, 13 in conventions.md Response 10 and 11, 7 in write a plan.md and 3 in pitfalls.md 22 to 24, still prose there today; and the breakdown of 16 September with its one undecided approach, the drafts taken back out of the truths and kept as the table. It has a db row. Nothing points at it: not the index, not a truth. For: the decided part lives in truth now, and the handbook says a proposal leaves the zone when it dissolves into truth; the section as written is stale, corrections.md being gone; the zone is the present, and this file is three days of past; git keeps every word. Against: the table is the only list of the 23 drafts and where each sits, and the drafts are still undecided, co's words inside three truths; the breakdown's one approach is written nowhere else; a log only grows, so what the file holds would need a dated entry in the work journal before it goes, or the record of what is still not Jonathan's in truth is lost to git alone. Decided: deleted, its table and breakdown a dated work journal entry first, the 23 drafts a question in zone/questions.md.
- 10 September 2026 — **call the new truth creating a library.** What exists: the rule decided today, four sentences on how a library is used — nothing built on its own, one alias per library per host in two files, one bridge per library per host, and a library that imports a library does the same — has no home. Its sibling-to-be is `create a project.md`, an imperative. The two truths nearest it are gerunds, core's `adopting core.md` and the zone's `incorporating a project.md`. The name offered first was `libraries.md`. For creating a library: it pairs with create a project, one for a project and one for a library, so a reader making one knows which to open, and a library is made only once while it is used many times, so the making is the rarer and easier-to-forget act. The file then also carries what a library must be, which the four sentences do not say: its own tsconfig and vitest config so it checks alone, no host vocabulary, state in the host, and its own bridge to any library it uses — the lessons adopting core.md paid for. Against: three of the four sentences are the host's side, taking a library on, and a host reading how to take one would not open a file called creating. The sibling is an imperative and this is a gerund; match one and mismatch the other. `libraries.md` says the subject and neither side. Middle path: `create a library.md`, imperative like its sibling, in two sections, what a library is and how a host takes one on, with create a project.md pointing at the second as a step. Cost, counted: one file of about twenty lines, one line in create a project.md, one line in adopting core.md saying it is core's case of the rule, and one lexicon entry, library, which the undecided library-term pac has waited on since 8 September. Deciding question: is the file about making a library or about using one — and does the name follow its sibling's form, create, or its neighbors', adopting and incorporating? Decided 10 September 2026: `libraries.md`, the subject and neither side, in two sections, what a library is and how a host takes one on. Moved to the zone the same day.
- 10 September 2026 — **libraries resolve through the workspace, not through aliases.** What exists: every host reaches a library through an alias written in three places that must agree, tsconfig's paths, vite's resolve, and a standalone vitest config where a host keeps one; adopting core.md calls this the wiring, and names the lesson paid for it, that a test runner reading a vitest config of its own needs the alias a third time. Seven tsconfigs carry the core alias and two the gallery alias. panel's and gallery's own files reach core the same way, through the alias of whatever host is building them, and through their own when checked alone. yarn has already linked core, panel and gallery into the root node_modules, since the root package.json names them as workspaces, and nothing reads those links. No library has an `exports` field, and every tsconfig allows an import to name its `.ts` extension. Nothing is published outside mono; lv and mj are built by vite from the whole checkout on Netlify. The proposal in zone/proposals.md: an `exports` map in each library's package.json, pointing at its source, in place of every alias. For: one map per library in place of three lines per host per library, and the lines that must agree are gone with them. A library's files resolve core on their own, from any host or none. It is the way TypeScript, vite and vitest already read a package, so no config of ours teaches any of the three anything. The bridges and the alias tests stay as they are: the specifiers do not change shape. Against: an `exports` pattern adds no extension, so either the map carries one pattern per kind of file, `.ts`, `.svelte` and `.css`, and a file named `.svelte.ts` is caught by the wrong pattern and fails to resolve, or every bridge line names its file's extension outright, which is 110 lines today across eight bridges. The alias is a plain prefix swap and has no such edge. And the links the map relies on are made by yarn's install at the root; whether Netlify installs at the root when the base directory is a host is unproved, and lv's and mj's builds work today without any link. Middle path: one pattern only, `"./*": "./src/lib/*"` for core and `"./*": "./src/*"` for gallery and panel, and every bridge line names its extension, `.ts`, `.svelte`, `.svelte.ts` or `.css`. One rule, no edge, and the map is a single line. Cost, counted: three maps of one line; 110 bridge lines each gaining an extension; the alias lines out of seven tsconfigs, seven vite configs and two vitest configs; the wiring section of adopting core.md and lv's structure.md rewritten; one deploy each of lv and mj to prove the link on Netlify, before the aliases go. Deciding question: is three agreeing lines per host per library a cost worth 110 extensions and an unproved Netlify link — and if Netlify's install does not make the link, does the deploy fail loudly enough to be caught before the aliases are removed? Decided 10 September 2026: dead. The aliases and the bridges stay, and a library that imports a library does the same.
- 9 September 2026 — **eliminate the outer div.** What exists: in each of four App.svelte files — ov, panel, mu, mj — the outer div carries the class `frame`, a style rule of fourteen lines (fixed at the window's top left, the accent, one gap of padding, a flex column, overflow hidden), and two inline values, its width and height, set from state: the window's height, and the wider of the window and `k.width.normal`, 300. index.html mounts the app at `#app`, so the div is `#app`'s one child. In ov it is drawn only after launch, inside the `{:else}`, while a fixed `.launch` box covers the screen before. The hits manager does not know the div. For: one element fewer, and no name to choose — the two renaming pacs and the naming proposal are moot. `#app` already is the app's box, so the fourteen lines move onto it through `:global(#app)`, as App.svelte already styles `body` and `:root`. Four hosts change alike. Against: a component would style its own mount point, an element it does not own, in a file outside itself. The 300-pixel minimum has no element to sit on: it goes into an effect that writes the style onto `#app` by hand, or into a css minimum from a pushed variable that core does not push today (only `--width-tiny` is), or it is dropped, and a window narrower than 300 squeezes the layout — to be measured, not reasoned. In ov, `#app` would wear the accent and the padding during launch too, under the launch box. And the result is checked by eye in four apps, ov's pixel by pixel. A middle path: do it in panel first, the one nobody runs, then mu and mj, and leave ov until the three are measured. Or keep the div and rename it `.app`, the proposal's first choice, which costs eight lines and moves nothing. Cost, counted: four App.svelte files, each losing a div and gaining a `:global(#app)` rule and a home for the minimum width, four checks, four screens looked at, four memory mentions of `.frame` rewritten, three pacs and a proposal marked moot. Deciding question: does a component own the styling of its mount point — and where does the minimum width go once no element carries it: an effect, a pushed variable, or nowhere? Decided 9 September 2026: the div stays, renamed `.app`.
- 9 September 2026 — **rename the class frame to the project's own name — ov, panel, mu, mj.** What exists: the same as the pac below. One class, `frame`, on the outer div in four App.svelte files, with the same style rule under it in each. Each host already holds its own name in one place, `customizations.name`, read by the controls row. For: the outer box of each app wears the app's name, so one glance at the markup or the styles says whose box it is. No word is shared, so nothing enters the lexicon and panel the project never collides with a class in mu. frame goes. Against: four names for one rule. The style block is the same fourteen lines in four files, and under four names a diff between hosts shows a change that is none. The name says which app, not what the div is — a box holding the three regions — where even frame said that much. And it cannot be set from the one place the name already lives: a scoped style needs its selector written out, so `.mu {` is typed by hand beside `name: 'mu'`, two places for one word. The Importing proposal, if taken, makes the box one component in panel with one class, and four names would fold back to one. A middle path: wait for the Importing proposal. Taken, the box has one name in one place and this pac is moot. Not taken, rename then, with the four versions known to be staying four. Cost, counted: eight code lines, four checks, four memory mentions. No lexicon entry. Deciding question: is the outer div named for the app that draws it, four names and one rule, or for what it is, one name everywhere — and does the Importing proposal, which would leave one box in one place, settle this by being decided first? Decided 9 September 2026: one name, `.app`, in every host.
- 9 September 2026 — **rename the class frame to panel.** What exists: `frame` is the class on the outer div in App.svelte — the box at the window's edges that holds the controls row, the details column and the operation view. It came with ov's first commit, 30 July 2026, with no reason on record. Eight lines in four files carry it: the div and its style rule in ov, panel, mu and mj. Four memory files name it as `.frame`: the indexes of panel, mu and mj, and shared's proposals. panel is Jonathan's word for the three regions together and is in no lexicon. For: one word for the thing and the box that holds it — the code, the prose and the project agree, and frame goes for good, a word Jonathan does not know. The class is the div's only name, so the rename costs nothing but the eight lines and the four mentions. Against: panel is already the project's name, so `class='panel'` in mu's App.svelte names a div after another project, and a reader must hold two senses of one word, a project and a box. The div is the box, not the three regions — it also holds the gap around them and, in ov, the status line below — so panel names the contents and the box would take the contents' name. And the class is code, which the lexicon says is called by its name, so a rename is a code change proved by four checks, not a prose change. A middle path: rename the class and enter panel in the shared lexicon with both senses said in one entry — the three regions together, and the div that holds them, whose class it is — so the word is defined once and the project is named after the thing it draws. Cost, counted: eight code lines, four checks, four memory mentions, one lexicon entry. Deciding question: does the box take the name of what it holds, so that panel is the three regions and their box together — and is a class named after a project a second sense the lexicon carries, or a collision? Decided 9 September 2026: not panel. The class is `.app`.
- 8 September 2026; **use "bridge" for Core.ts — the host's one file that imports what it needs from a library.** What exists: bridge is already a name, unlisted, for a different thing. core's own `Configuration.ts` says in its opening comment "This is the bridge that lets CSS use the numbers"; the ov goals truth, in both ov's and core's truth folders, says "`Configuration.ts` — this is the bridge: it pushes every number from Constants onto the page"; ov's map says the same of main.ts; and the entry file of four hosts — lv, mj, mu, panel — opens "The bridge from core's one source of sizes to any stylesheet", with mu's and mj's Operation.svelte saying "the bridge from Constants to the page". Eleven places, one sense: the crossing from a typescript number to a css variable. A third sense sits in claude code.md, "a bridge to the Mac's files". For bridge on Core.ts: it says the shape — the host on one side, the library on the other, Core.ts the one crossing, and every other host file walks over it rather than reaching the far side itself; it says which way things travel, from the library into the host, which door and import both left unsaid; and it is an everyday word with no code sense to collide with, where import names the statement. Against: the word is taken, in core's own source and in eight host files, for the number-to-page crossing — so bridge on Core.ts is one word for two crossings, which the lexicon forbids, unless Configuration's sense is renamed first; and that sense is the older, written into the truths on 31 August and into every host's entry file since; door's define and four-doors senses are untouched by bridge, as by import, so door survives anyway. A middle path: give Configuration's crossing its literal name first — the adopting-core truth already says it in plain words, "pushes the constants and inks onto the page" — and free bridge for Core.ts, so each crossing has one word; or the reverse, keep bridge where it is and call Core.ts by its name, with no word for the kind until a second library's file needs one. Cost, counted: for Core.ts, the same twenty memory lines and four test lines the door pac counted, plus one lexicon entry; for freeing the word, eleven lines — three truths, core's comment, four entry files, two Operation comments — each rewritten to say pushes onto the page. Deciding question: which crossing is the bridge — Core.ts, library into host, or Configuration.ts, numbers onto the page — and does the other get its literal name in the same write? Decided 8 September 2026: the middle path. Core.ts is the bridge, and Configuration is configuration — its eleven bridge lines now say what it does, pushes onto the page. The library sense of door is swept to bridge; the define and four-doors senses keep door.
- 8 September 2026; **replace "door" with "import".** What exists: door is used in three senses, none in a lexicon. (1) The library sense — the one file in a host that reaches through a library's alias, Core.ts for core: adopting core.md says "the doors are two, and both are named"; core's decisions has a pac titled "the stylesheet's door"; ov's `core_alias.test.ts` names a `doors` map, a describe called "the doors through the core alias", and a comment "where a third door fails it"; and this week's writing in panel's index, mu's and mj's logs, ov's index and log, incorporating a project.md and the panel proposals says door about twenty times. (2) The define sense — the handbook's and shorthand's "the lexicon's only door", the define skill as the one way a term enters. (3) The four-doors sense — the life-cycle proposal and four August pacs in this file, the four places an idea could wait. For "import": it is the word the code already uses on every host file that takes from Core.ts, and the word a reader of any host meets first; door is a picture, and the conventions ask for the literal fact where one exists — the Hamburger pac was decided on that line; the door file's whole job is bringing a library in, which is what import means in everyday English too. Against: "import" already names the JS statement, so "the import" for a file and "an import" for a line are one word for two things, which the lexicon forbids outright; Core.ts's lines are `export { x } from 'core/…'` — re-exports, so "import" is not even the literal act on that file, "re-export" is; senses (2) and (3) cannot take "import" at all, so door survives the sweep anyway and the rule gains a word without losing one; and the test's `doors` map and titles are code, changed and re-run, for a rename that touches no behavior. A middle path: say the file's own name where there is one — Core.ts, as the lexicon's opening rule already asks — and for the kind, "the one file that reaches through the alias", which adopting core.md already says in those words; no new term, door kept for senses (2) and (3) only, or dropped there too by separate decision. Cost, counted: about twenty lines across ten memory files for sense (1), four lines of test code, one lexicon entry either way — and, for "import", a second word to tell the file from the statement. The middle path is the same sweep with no entry. Deciding question: is the thing named by what it does to the host — brings a library in, import — or by what its lines do — re-export — or by its name, Core.ts, with no word for the kind until a second library's file exists to need one; and do the define and four-doors senses keep door? Decided 8 September 2026 by the bridge pac above: the library sense is bridge, and the other two senses keep door.
- 8 September 2026; **library — a lexicon term for core, panel and gallery, as in importing a library.** What exists: the word is already in use, unlisted — core's index calls core "the library every mono app shares", its structure truth opens "A library with no entry point" and wears `library` as a tag, and six vite configs (ov, lv, gallery, mj, mu, panel) carry the same comment, "core is an alias for the shared library one folder over". The lexicon has **adoption** — a core file taken in by a host, one line in the host's Core.ts — but neither *library* nor *host*, though every core truth says host. The banned-words table has no library row. The three named differ in kind today: core is imported, through the `core` alias, by six hosts; panel is not imported by anyone — the decision of 7 September is that a host takes panel's four files as its own, and a host does not import panel; gallery is lv's whole code duplicated and renamed on 1 September, with no port, no alias in lv's vite config and nothing in lv's src imported from it — a duplicate of an app, meant to be taken apart into what lv adopts. For: one word for the kind of project that exists to be taken by others, where today there is one name (core) and no name for the kind; it pairs with host, which the truths use on every page and the lexicon never defines; and the word is the everyday one — "import a library" is what a reader already says. Against: only core is a library in the import sense today; naming panel and gallery libraries names what they are meant to be, not what they are, which is the fault of 7 September in a new coat — unless the entry says a library is a project made to be taken, whether by alias or by file, and then panel fits and gallery is a library-to-be; and the word carries a second everyday sense, the packages in node_modules, which the adopting-core truth calls dependencies, so the entry has to say which sense is meant and the other stays plain. A middle path: enter *library* and *host* together as a pair, since neither means anything alone — a library is what a host takes, a host is what takes a library — and let the entry name core as the one library today, panel as one a host takes by file, gallery as not yet one. Cost, counted: two lexicon entries, one D: line, and a read of core's truths for any sentence the entries make wrong (none found: they already say library and host in this sense). Deciding question: is a library a project others import through an alias — core alone — or any project made to be taken, by alias or by file — core, panel, and gallery once it is taken apart; and does *host* enter with it? Decided 10 September 2026: a library is any project made to be taken, core, panel and gallery, and host enters with it. Both are in the lexicon, and the rule for every library is `zone/architecture/libraries.md`.
- 6 September 2026; **remove all explanations from co's replies.** Three rules already cut the unasked ones: always #5 cuts an explanation or an unasked reason, conventions line 21 says a question gets its answer and nothing else, response #9 step 3 keeps a reason only where the reader would otherwise stop and wonder. For: a blanket ban turns three judgments into one hard check — does the reply hold a because, a so-that or a since clause — the refinement conventions asks of every judgment rule; explanations are where replies grow and where guesses ride, since a reason is rarely a thing co read this turn; and it is the evidence rule again — hold the reason, say "I can prove this", give it when asked. Against: a why is answered by an explanation, and Jonathan asked why five times on 5 September alone — `x` and `explain` exist to ask for one; a report carries the reason a thing waits (conventions line 34) and the first principle says surface tradeoffs, and a tradeoff is a reason — two collisions; and the one fact that makes a "no" checkable ("nothing to change: the code already reads --text") is an explanation by form, and without it a no reads as a refusal. Cost, counted: one line in always #5 or conventions line 21; one pattern in the murk hook. Deciding question: is the ban on unasked explanations — the rule as it is, made a hard check — or on every explanation, the answers to why, the tradeoffs and the reasons a thing waits included? Decided 6 September 2026: the unasked ones — explain only when asked why, and then well; conventions line 21 says it in Jonathan's words.
- 3 September 2026; **close this session now.** Two days across one compaction: the always rewrite, four hooks, the sweep, its review, and two settles — the second because a discard undid the first. For: everything is committed — nothing is uncommitted but the hook counter; both logs are settled to 3 September, with an S: line naming every dropped line's home; the rules files hold the day's lessons; nothing is in flight; the next session opens on two questions already written in zone/questions.md — verify this settle, and what the starved-row test showed. Against: checked findings 4, 5 and 7 are open again, their fixes lost in the discard; the sweep branch and its worktree still exist for Jonathan to delete; the context is long enough that a stale read is likelier each turn — which argues for closing, not against. Cost of closing now: none that a commit did not already pay. Deciding question: none left — the commit was the only condition, and it is met. Decided 3 September 2026: close.
- 3 September 2026; **write the sweep-review lessons into the rules files.** Jonathan reviewed the mechanical sweep and reversed about 60 of its 260 word changes; every reversal was one fault — a table row applied by word, not by the sense its Meaning column names. The proposal puts the sense boundary into the file that owns each word: the lexicon's button/decoration, gap/room, more-work/heavy-lifting and main/global entries; one sentence atop banned words.md saying a row with several replacements is chosen by meaning, none the default, plus a narrower ship row; one learn.md entry; and the same sense rule into the sweep plan. For: the next sweep — and every hook that reads the tables — inherits the boundary instead of rediscovering it; the six edits are small, each in its one home, and four of them are checkable at once by rereading the sweep's own reversals against them; "room" on plain-english-check's skip list stops a hook complaining twelve times about a word it cannot judge. Against: sharper entries are longer entries in a lexicon injected every fourth turn, and a sense boundary is judgment — a script still cannot tell "room to spare" from "a room-wide gap", so the hook gains nothing and the skip list loses a real catch; the main/global entry is a guess at Jonathan's meaning and could enshrine the wrong distinction; the learn entry repeats what the lexicon entries already say, against say-it-once. Cost, counted: four lexicon entries reworded, one banned-words header sentence and one row narrowed, one skip-list word in a hook, one learn entry, one sentence in the sweep plan — nine edits, no code. Deciding question: does main/global need Jonathan's own words before anything is written — yes; the other five carry no guess. Decided 3 September 2026: "global" everywhere, "main" only as the branch name — all six edits made.
- 31 August 2026; **move `questions.md` into `zone/`.** Weighed once already: the 29 August pac asked this bundled with `log.md` and was never decided, its deciding question left open — is zone "ideas before belief", as the lexicon then said, or "everything not yet truth"? Two things have changed since. Zone was redefined without a formal sweep — `shared/index.md` now reads "zone means a project's live thinking — ideas.md for ideas, proposals.md for proposals, drive.md for the one being driven" — and a proposal is not an idea before belief, so the wider reading already won in practice. And this ask is the narrower half: the log stays at the root, which drops the old pac's strongest objection, that a log is a time layer and a peer of truth/, not a maybe. For: a question is unsettled by definition, and zone is the one door for the unsettled — the root then reads as three plain layers, what is believed (truth/), what happened (log.md), what is not settled (zone/). A question usually feeds the drive it sits beside, and today proved it: every question in shared moved by hand into `zone/drive.md`, so the two were already one thing. The handbook's "the file exists only while questions do" also reads better inside zone, where files come and go by design, than at a root where a vanishing file looks like breakage. Against: the file was put at the root to be seen — "questions are not history, and a log buries them" (handbook line 18) — and start reads the index, the lexicon and the matched truths, never the whole zone folder, so a question in zone is a question nobody opens. One name, one concept cuts the same way: live thinking is thinking being done, and a question is thinking stopped, waiting on Jonathan; admitting it needs zone formally redefined through `define` and swept, which the 29 August pac already named as the price. The root shrink is small — four doors become three, not one. The cost, counted: four files move (shared, mu, me, lv), and five pointers re-point — handbook lines 18, 20, 27 and 106, and `shared/index.md` line 8. Shorthand names no path, so it needs nothing. Deciding question: today every question in shared was moved by hand into `drive.md`. If that is the real answer, the question is not where `questions.md` sits but whether it exists at all — does a project keep a separate waiting list, or does each question ride the drive it belongs to? Decided 31 August 2026: the separate waiting list keeps its life and moves — all four questions files are now `zone/questions.md`, and zone holds four named files: ideas, proposals, drive, questions.
- 31 August 2026; **working.md as the name for the proposals-in-flight file.** For: the gerund says activity, not state — these proposals are being worked, right now, which is truer than open or active. The idiom carries it: a working draft, working papers — provisional things mid-shaping, exactly what the file holds. And unlike the three state-words, it survives its neighbors: no tag, no index paragraph, no ov control wears it. Against: as an adjective it also means functioning — "a working app" — so working.md could read as "what works" rather than "what is being worked." The word's family is all over the dying old notes (notes/work/, work journal, working features), so the name drags an old smell into the new design. And it still says how rather than what — the contents question ("where is the plan for X") is answered by proposals.md, not by any word about activity. Deciding question: does the working-draft idiom carry the meaning cleanly, or does what-it-holds beat how-it-stands whatever the word? Decided 31 August 2026: what-it-holds won, with a sharper word — `drive`.
- 31 August 2026; **name the zone file that holds proposals in flight: current.md, open.md, or active.md.** For current: says the work is the work of now, and pairs with the index's "current state" — the paragraph tells where the project stands, the file tells what is being built. Against current: that pairing is the trouble — "current" already belongs to index.md's paragraph, and a file wearing the same word makes two claimants on it; the where-to-look answer becomes a coin flip. For open: honest about state — a proposal in the file is not closed — and short. Against open: the word is loaded everywhere nearby: questions.md was "open questions" days ago, ov opens files and folds, and an "open.md" reads as a verb waiting for an object. For active: says exactly what zone means — live thinking, being acted on. Against active: "active" is already a tag area in ov (now, next, soon, later, tabled) — the same word naming a file and a tag family is the duplication bug in word form, and the freshest convention says name the concrete thing. All three describe the state, not the contents — proposals.md says what is inside, these say how it is doing; a reader hunting "where is the plan for X" asks for contents. Deciding question: should the file's name say what it holds (proposals) or how its holdings stand (current, open, active) — and can any state-word survive its collisions? Decided 31 August 2026: none of the three — the file is `zone/drive.md`, named for what it holds: the current drive, a proposal being decided and implemented.
- 30 August 2026; **move the map files into truth/ — each project's file map becomes its `truth/structure.md`.** For: a map states what is — every file and what it does — which is a truth's job (structure.md is not a map — it tells core's design, not what each file is; a map keeps its own name). The family is real: seven file maps across di, ji, lv and ov (files, guides and notes maps), all load-bearing ("read this instead of hunting"), all commanding truth discipline in their own descriptions. One decided pattern settles all seven, and core's copy of ov's map is a carve leftover to delete, not move. Against: the sizes break the truth cap by multiples, not lines — ov's map is 37.6k, ji's 27.9k — so moving whole is impossible and trimming is the real work. The update duty is a hand-kept promise, the drift the lifecycle pac already named, and a stale map misleads worse than hunting. di has no memory project to receive its three maps. And the road maps (di, ji) wear the name but are plans, not file maps — zone material if they move at all. Deciding question: trim each under the cap at the door, or move whole and let settles cut — and does a 37.6k map belong in memory at all, or should ov's app answer "what does this file do" instead? Decided 31 August 2026: moved whole, under their own names, each into its project's truth/ — settles cut them, and the new settle step keeps them true from git; core's copy of ov's map is deleted; the road maps stay where they are, being plans.
- 30 August 2026; **remove the logs from ov.** For: logs are mined and fed to settle, never read — and ov is an instrument for reading and grasping. Every log row in browse is noise in that instrument: ~8 files that no filter stage claims (logs ride no life cycle), whose content is one-line entries meant for sweeping. Removing them sharpens what ov is for. Against: today's date mistake was caught by reading a log in ov — the rare look does happen, and the editor is where a log line gets hand-fixed. Hiding files makes ov lie about what a folder holds, the claim a browse table exists to make. And the lifecycle area may already do the job gently: logs wear no stage tag, so stage-filtered views omit them without anyone hiding anything. Deciding question: hidden from the table, or merely absent from every stage filter — does ov show what is, or what is worth reading? Decided 30 August 2026: hidden — browse neither shows nor counts log files.
- 30 August 2026; **add the lifecycle tags to the memory files.** For: the instrument lights only when files wear the tags — ov's lifecycle area filters nothing until then. One tag per file over ~40 files is one cheap sweep, and the big picture arrives at once: waiting's count shows today's true backlog. The stages were designed against these exact files, so most placements are obvious — questions.md waits, truth/ is incorporated, raw zone is born. Against: a sweep bakes in ~40 guesses in one act, and a wrong settled misleads more than no tag — the prior pac's drift warning remains unanswered, since nothing yet audits a tag at settle. okf's closed-list rule is satisfied, but its deeper rule — every placement true — is what a mass sweep risks. Tagging file by file, each at its next touch, earns each tag instead. Deciding question: one sweep now, or each file earns its tag at its next touch — and which act (settle?) audits the tags so they stay true? Decided 30 August 2026: one sweep, now — every cycle-riding file wears its stage; logs and indexes ride none; the settle audit stays open.
- 30 August 2026; **per mono project, merge all code debt content into memory, the paid subsection's content going to the log under an H2 "code debt paid (transferred, <today's date>)".** For: it answers the open deciding question — the done section neither transfers into zone nor dies: paid debt is history, and the log is the history file (truth = is, log = recent, git = was). Each kind lands in its nature's file: unpaid debts become live ideas with a settle pulse, paid ones become record. Precedent exists — ov's log already carries the "## original work log" appendix, kept verbatim. And the H2 names the transfer and its date, so the sweep knows it is imported record, not a day's work. Against: the log's law is one-line tagged entries under day headings, emptied by settle — a ~315-checkbox H2 is a boulder every consolidation steps around forever, permanent fat in a file meant to drain. Git already holds each paid item; the transfer writes that history a third time. And this is the third pac claiming the code-debt topic — the pile of rivals now needs a d more than a new evaluation. Deciding question: is the log temporary and emptied, or a ledger where record may sit — which does the appendix precedent make it? Decided 30 August 2026: no transfer — the done sections die at the door; the log stays temporary.
- 30 August 2026; **per mono project, merge all code debt content into the memory project's ideas.md, at the very top — after the H1 and a blank line.** For: a debt is a stage-1 idea — work someone might do — and ideas.md is where the life-cycle proposal gives such things a pulse: triaged every settle instead of rotting in a file nobody re-reads. One zone file fewer per project. Leading the file, the debts meet the triage first — obligation before musing. And it reaches beyond the twins: ji and lv still keep code debt only in the old notes's notes/work/, which this transfer would finally move in. Against: ov's file is 406 lines, ~315 of them finished checkboxes — transferred whole it drowns ideas.md, and importing history is what inception forbids; the done section should die at the door, git keeping it. The cull rule still menaces owed debts (the parent pac's unresolved Against). Position as meaning is a hidden rule — "top = obligation" is invisible; a named section says it aloud. And the parent pac — merge code debt *and handoff* into ideas — still waits: two pacs now claim one topic. Deciding question: does the done section transfer too, or die at the door — is this a merge or a triage? Decided 30 August 2026: it dies at the door — unpaid content leads each memory project's ideas.md, git keeps the paid.
- 30 August 2026; **name the gathered decision stories "use cases."** For: the term is already alive in the memory — core's design.md holds "use case: Constants" and "use case: Configuration" — and it is the standard software term, which `translate` now prefers over a coined one; it needs no definition. Against: the standard sense points forward — how a thing is to be used — and design.md uses it exactly that way. The gathered stories point back: a question, its answer, what now holds. That is a precedent, not a use. One term carrying both senses across two files is two claimants on one word — the duplication bug in word form. Deciding question: is a decided story a use case or a precedent? Decided 30 August 2026: neither name — the file is `cases.md`; "use case" stays design.md's, forward-pointing.
- 30 August 2026; **remove the "avoid technical terminology" rule, hoping to fix murky and wordy.** For: the rule lives in `translate` ("Avoid jargon") and today produced both faults it exists to prevent — dodging "dependency cycle" forced "form a circle," a metaphor murkier and wordier than the term. A standard term is one word doing the work of a clause; precision is compression. And word choice already has a governor with a finer instrument: the lexicon coins terms, the banned-words table bans them, Jonathan edits both — a blanket avoidance is a second claimant on that topic, the duplication bug in rule form. Against: the rule's target was never standard terms but decorative ones — utilize, leverage, orthogonal — and removing the whole rule readmits those. The session's actual murk came mostly from home-made words where standard terms existed (circle, edge, nickname, "decided treatment"), which removal does not touch: the banned-words table fixed those, one by one. And the murk rate is measured (`rate`); the hope could be tested instead of trusted. A middle path: reword rather than remove — "use the standard term; avoid the decorative one" — and leave `plain` as the no-undefined-terms door for when it is asked for. Deciding question: is the rule's target technical terms, or decoration wearing them? Decided 30 August 2026: decoration. The rule now reads "use the standard term; avoid the decorative one"; `plain` remains the defined-terms-only door.

