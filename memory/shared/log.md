---
kind: analyze
title: "shared log"
description: "What shared decided, thought and reached, newest first; settled entries leave at each consolidation."
tags: [journal, now]
date: 2026-09-01
---
# shared log

<!-- consolidated: 3 September 2026 -->

## 7 September 2026

- D: a logs/ folder inside each memory project, decided and built — twenty-two files moved from `mono/logs/` into `memory/<project>/logs/` (di, ji, ov, lv, mj, ma, ga, ws, core), three old-named ones with them (designintuition.log and dimensionals.log to di, intersection.log to ji), plain `mv` since `.gitignore`'s `**/logs/` line meant none were ever tracked; `s3.log` and its error log deleted, s3 being gone entirely; eleven unmatched files left at `mono/logs/`; servers.sh's `start_site`, the dispatcher's `/log` route and `DOC_ERROR_LOGS`, and update-project-docs.sh's error log all re-pointed by project name; conventions.md's response rule 5 reads the new place; a live write confirmed, ov 336 tests, svelte-check clean, dispatcher 32 tests
- D: cleanup, run after Jonathan reorganized the memory folder himself in Obsidian — every project's `notes/` folder is now gone entirely, its guides and designs in `truth/` (or `zone/designs/` for ji), its work in `zone/work/`, ov's design-derived truth files gathered under `truth/design/`, `ov/notes/work/AI memory redesign.md` moved to `memory/core/zone/work/`, `ov/truth/scope.md` folded into `ov/index.md`; `memory/s3/` deleted entirely, confirmed intentional by Jonathan; git's own rename detection supplied the mapping (635 renames, 55 confirmed pure deletes, 20 additions); 75 markdown links and 9 wiki-links re-pointed, each verified against HEAD so nothing pre-existing or merely dead was touched; di's, ji's and ws's project CLAUDE files, the root CLAUDE.md, inject-always.sh, shorthand.md, gates.md and handbook.md re-pointed from `notes/work` to `zone/work` and `notes/guides` to `truth/`; the dispatcher's listing and `is_listed_note` rewritten around one rule — `zone/work` depth-limited, everything else under memory/ readable at any depth — its test's example paths moved with it, dispatcher restarted; ov 336 tests, svelte-check clean, dispatcher 32 tests; not touched: ov's own `Saving.ts` still roots a collection's files at a `notes/` folder that no longer exists anywhere, so every file now falls through to the generic memory case and `is_design` is always false — di's and ji's road maps lose that flag; a real gap, not caught by any test, left for Jonathan to say how `is_design` should be told now that guides, designs and work are no longer three named notes/ subfolders
- D: the whole s3 project is gone too, confirmed intentional — 73 source files at the repo's top, beside its memory folder; package.json's workspaces list, tools/hub/ports.json's entry and tools/hub/servers.sh's three s3 mentions (a port variable, a dev-server row, the target-name switch) removed, so yarn and the hub no longer name a project that isn't there
- D: `cleanup` is a shorthand — after files or folders move inside memory: re-point every link the move broke, fix CLAUDE.md, the hooks, shorthand.md, keywords.md, gates.md, the project's map and index, drop what emptied, run vitest, svelte-check and the dispatcher's test, log it with a D: line; the row is in truth/shorthand.md, zone/proposals.md marked built
- D: except mono's own, every project's notes folder moves whole into memory — `<project>/notes/` becomes `memory/<project>/notes/`, twelve folders, 419 files, four of them projects with no memory folder yet (ga, ma, s3, project template); this reverses the inception rules "pull, don't push, never bulk-import" and "journals and handoffs stay behind in place", rewritten in the handbook; the plan is in zone/proposals.md; built the same day, below
- D: mono's own notes follow the same move — `notes/guides`, `notes/work`, `notes/archives` and `notes/index.md` sit at `memory/shared/notes/`; `notes/tools` (the hub dispatcher, the docs scripts, ports.json) stays at `notes/tools/`, co's call as infrastructure, asked in zone/questions.md
- D: the move is built — thirteen `git mv`, history kept; every reader re-pointed: ov's Saving.ts roots each collection's notes at `memory/<project>/notes/` (mono's at `memory/shared/notes/`) and reads a collection's notes before the memory rule; the dispatcher lists and reads by one `COLLECTIONS`/`CLAIMED` pair, so a claimed collection's notes keep the notes rules inside memory; five hooks, CLAUDE.md, eight project CLAUDE files, the cd command, the hub page's revisit paths; 436 links in 48 files re-pointed by resolving each against its file's old place, 52 prose mentions and wiki links, lv's test comments; ov 336 tests, dispatcher 32, svelte-check clean — the dispatcher test named five files that were already gone (protocol.md, handoff.md, organize.md, worth it?.md, di's learn.md) and now names files that exist
- D: `notes/tools/` is `tools/` at the top of the repo, and `notes/` is deleted — `git mv`, history kept; the dispatcher restarted from its new place; its readers re-pointed: eleven vite configs' ports.json import, the dispatcher's docs-script path, servers.sh, the tools' own scripts, di's `yarn next` (its sync script moved with di's notes), the README, ov's code-link test, four permission strings, the hub guide, the netlify and vitepress guides, create a project.md, the handbook's inception bullet; `~/.zshrc` lines 1 and 20 still say `notes/tools`, outside the repo, Jonathan's to edit
- D: the stale content left after the notes move is set right — core's CLAUDE names core's own lexicon and says core has no file map; the ov map's ji sentence points at ov - goals.md, which the proposal became; s3's subsystems index drops its mention of a ws note that is nowhere; di's Debug.ts comment names `memory/di/notes/work/mothballs/dimensionals.work.md`, its Dimension_Placement.ts comment drops a bug note that is nowhere; prebuild.sh copies from `memory/<project>/notes`
- I: pac — move every working features file and every pre-flight file into truth/; weighed in truth/decisions.md, undecided; the deciding question is whether the three shared pre-flight files with a truth already fold in or sit beside it
- D: fold — every working features file (six) and every pre-flight file (twenty-one) is in truth/: always, response and the banned words are sections of shared's conventions.md; the mono-wide lexicon is part of shared's lexicon.md; agency, gates, keywords, kinds of tasks, pitfalls, shorthand, di's always, lexicon and banned words, ji's lexicon, and ov's and core's banned words moved whole; the five pre-flight index files are gone; the pac in truth/decisions.md is decided
- D: what the fold dropped from conventions.md: ten prose bans that repeated a table row (land, stand, shape, edge, drain, pour, borrow, owes, circle, step/move), the evidence and rosy bullets that repeated always #4 and #3, and the compress and asides bullets, now part of response #6 and #3 — checked findings 4 and 5 in zone/questions.md are answered by it
- D: inject-always sends conventions' Always section every turn and the rest of conventions, agency, the lexicon and the project's banned words in rotation; the four word hooks and test-always-tag read truth/, its four checks hold; the stray scan now covers truth/, which named handbook.md — its `always` tag is `session`, since start loads it and no hook sends it; CLAUDE.MD's two lines, the handbook's shorthand line, keywords' four rows, the hooks guide, the hooks proposal, the maps of ov, di, ji and lv, seven catalogs, ten old indexes and 33 links in 11 files re-pointed; ov 336 tests, dispatcher 32
- I: pac — move every project folder into `projects/`; weighed in truth/decisions.md against the 1 September proposal in ov's zone, undecided; the deciding question is whether four named top-level folders are worth the Netlify base-folder edit for lv and gallery and twelve kinds of reader
- I: pac — sub-folders in truth/, for the 89 guides entering it under ov's zone/consolidate.md; weighed in truth/decisions.md, undecided; the deciding question is whether the design's "truth/ never grows subfolders" holds as a principle, the folder names moving into tags, or is rewritten
- D: `truth/` may hold sub-folders — "truth/ never grows subfolders" is gone from the rationale doc, with its three sibling sentences that said flat; the handbook's design-churn rules say a folder inside truth names a design's parts and every lookup reads the folders too; the pac in truth/decisions.md is decided; consolidate.md's first question and the drive's are answered
- D: the six shared guides subfolders moved whole into `truth/` — collaborate, develop, philosophy, setup, test, tools — and ws's four — architecture, collaborate, manuals, plus di's and ji's and core's and ov's, 19 folders and 185 files across seven projects; `git mv`, links re-pointed, six catalogs gain one line per folder, the inception file's collaborate line updated and a line added for the rest; the fold/archive/dead sorting in ov's zone/consolidate.md is not yet carried out — every file sits in truth/ where its guides folder landed
- D: the handbook's inception section reads the move as done: a moved folder is still the old notes, its truths enter `truth/` one at a time the day work reaches for them; only `notes/tools/` remains outside

## 6 September 2026

- D: the lifecycle tag `settled` is gone from ov's closed list and lifecycle area — the lifecycle is four tags, born, weighed, waiting, incorporated; File.ts, Tag_Areas.ts, ov's controls.md line 21; the life-cycle proposal's fifth stage remains the settle act, not a tag
- D: the five files wearing the lifecycle tag `settled` wear `incorporated` — lv's gallery, ov's scope, action type and claude-files, shared's taste — so every truth file wears the one endpoint tag; `settled` remains in ov's closed list and lifecycle area with no wearer, reported
- D: co explains only when asked why, and then well; not asked, nothing — it crowds the good stuff; conventions line 21 says it in Jonathan's words, the pac in truth/decisions.md is decided

## 3 September 2026

- S: settled 34 lines of 1, 2 and 3 September into their homes — every rule change into the guide it changed (always, response, agency, lexicon, banned words, pitfalls, conventions); every hook change into hooks.md's rows and the scripts themselves; the sweep into notes/work/big rewrite log.md and the pacs in truth/decisions.md; the global-not-main decision and the sweep-review lessons into the lexicon and the banned-words table; the two-model strategy left in zone/fable.md with its pac undecided; two finished acts (fifteen entries settled, six descriptions filled) dismissed as done. always #8's quiet-turn wording is Jonathan's later "hooks report clean", not the logged "all good". Verification of this settle waits for a new session, asked in zone/questions.md
- S: open — checked findings 4, 5 and 7 in zone/questions.md; the sweep branch and its worktree await Jonathan's deletion; the starved-row change tested bad by Jonathan, co's unwiring of it discarded with the day's uncommitted edits — asked in zone/questions.md
