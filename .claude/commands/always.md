Run `bash ~/GitHub/mono/.claude/hooks/test-always-tag.sh` and report what it says.

It proves that the guides arriving with every message and the guides wearing the `always` tag are the same set. It breaks each half in turn and puts it back, so a pass means the check is actually looking rather than merely silent.