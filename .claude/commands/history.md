List the last N queries Jonathan typed in this chat, newest first, one per numbered line, each verbatim and whole. N is the argument; when it is missing, N is 5.

A query is a message Jonathan typed. Text a hook added, a skill's own instructions and system notes are not queries, and neither is this /history call itself.

ARGUMENTS: $ARGUMENTS
