Pick the project to work on. The argument is its short name — `/p ov` picks ov.

Write that name, and nothing else, to `~/GitHub/mono/.working_project`. Refuse any name that is not a folder at `~/GitHub/mono/<name>/`, and say which names are there instead.

Then say in one line which project is picked. Read nothing else and offer nothing.

With no argument, say which project is picked now.
