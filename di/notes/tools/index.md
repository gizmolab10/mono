# Tools

Scripts that support the build and the notes.

- `sync-next.mjs` — reads the first unchecked item out of the code-debt list and writes it into the handoff's Next section, so the two never drift apart.
