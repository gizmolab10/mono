# CLAUDE

> the library extracted from ov, which ai and mu import: the files list, the filter sections, the operation view, the details column, and the page's side of the db. ov stays as it is, the reference kb is proved against.

Made 12 September 2026 at step 4 of the plan in `memory/ai/zone/work/music and ai.md`, holding all of ov's code: its page under the name Main.svelte, which a host's App.svelte draws, and no entry file, since a library has none. Since step 5, 13 September 2026, the page draws panel, and a host hands kb its facts in `common/Customizations.ts` and its drawing as four snippets, as `memory/ai/zone/work/adopt kb.md` says. Each step of the plan that follows moves a piece that is the ai specialty's out to the ai host, until ov is retired.

Read `memory/kb/truth/lexicon.md` at session start. It names everything in this project: use those words in prose, comments, log lines and test names, and invent no synonyms.

The file map is `memory/kb/truth/map of kb files.md`. Read it instead of discovering files using regex and wildcards, and update it when files are added, moved, or removed.

kb is a library, as `memory/shared/zone/architecture/libraries.md` says: it names nothing of any host's, it checks and tests alone, and it reaches core through `common/Core.ts` and panel through `common/Panel.ts`, nothing else reaching through an alias.

Past mistakes never to repeat, this project's own, are in `memory/kb/zone/learn.md`, made the day the first one is written. Those that apply everywhere are in `memory/shared/zone/learn.md`.
