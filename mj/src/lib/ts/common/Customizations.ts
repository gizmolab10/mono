// What is true of mj and of no other host. core knows none of it, and nothing here is
// remembered between visits — a viewer's own choices belong wherever mj comes to keep
// them, and the sizes and colors are core's. If core could ever want it, it belongs to
// core; if a viewer can change it, it is remembered; otherwise it is here.
//
// Gathered into one value, so a caller names the file rather than every switch.

export const customizations = {
};
