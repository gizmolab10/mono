<script lang='ts'>
  import { k } from '../ts/common/Core';

  // The whole app on day one: one line saying it is running, drawn with core's own
  // sizes so the bridge from Constants to the page is proved the moment it opens.
</script>

<main style:padding='{k.gap.fat}px' style:font-size='{k.font.normal}px'>
  mj is running.
</main>
