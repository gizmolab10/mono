// What is true of ai and of no other host. core and kb know none of it, and nothing here is
// remembered between visits. Gathered into one value, so a caller names the file rather than
// every value. Step 5 of the plan hands it to kb as the configuration; until then kb's page
// reads none of it.

export const customizations = {
	name : 'ai',		// what the controls row calls this project
};
