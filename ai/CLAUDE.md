# CLAUDE

> a browser and editor for every markdown file in this repo, the memory system's, drawn by kb: the host that brings the ai specialty.

Made 12 September 2026 at step 4 of the plan in `memory/ai/truth/design/how ai hosts kb.md`, an empty host: main.ts does what ov's does, App.svelte draws kb's page, Customizations.ts holds the name, and the bridges, `common/Core.ts` and `common/Kb.ts`, are the only files that reach through an alias, with `common/Convert_Preferences.ts` since step 9, for kb's customizations alone, ahead of kb. Each step of the plan that follows moves one piece of the ai specialty here from kb. ov is frozen: every further change is made here, or in kb once the piece is kb's, never in ov.

Read `memory/ai/truth/lexicon.md` at session start, and kb's beside it, `memory/kb/truth/lexicon.md`, since ai's page is kb's. They name everything in this project: use those words in prose, comments, log lines and test names, and invent no synonyms.

The file map is `memory/ai/truth/map of ai files.md`. Read it instead of discovering files using regex and wildcards, and update it when files are added, moved, or removed.

Past mistakes never to repeat, this project's own, are in `memory/ai/zone/learn.md`, made the day the first one is written. Those that apply everywhere are in `memory/shared/zone/learn.md`.
